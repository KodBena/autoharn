#!/bin/sh
# DEPRECATED alias (design/FABLE-AUTOHARN-UMBRELLA-CLI-SPEC.md §6, ledger rows 1151-1183):
# ./distance-to-clean is now a one-line shim over `autoharn distance-to-clean` -- removed at the
# first post-2.0.0 minor. Semantics, refusal texts and exit codes are unchanged; only the
# invocation spelling moved.
echo "distance-to-clean: DEPRECATED spelling -- use 'autoharn distance-to-clean' instead (this alias is removed at the first post-2.0.0 minor, design/FABLE-AUTOHARN-UMBRELLA-CLI-SPEC.md §6)." >&2
exec "$(cd "$(dirname "$0")" && pwd)/autoharn" distance-to-clean "$@"
