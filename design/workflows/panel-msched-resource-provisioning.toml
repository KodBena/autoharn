# >>> PROVENANCE-STAMP >>> (auto; tools/hooks/stamp_provenance.py — do not hand-edit)
#   first-seen : 2026-07-16T02:43:57Z
#   last-change: 2026-07-16T02:43:57Z
#   contributors: 9a17b6b9/main
# <<< PROVENANCE-STAMP <<<

# Specimen A -- transcribed from the autoharn-panel deployment's ledger
# (~/w/vdc/1/experience/autoharn-panel), rows 14-47: the maintainer's commission (row 14) to
# provision makespan-scheduler as a blessed resource, decomposed live into a three-item pipeline
# (rows 15-20), reviewed (rows 21, 29-31), implemented and independently verified per item
# (rows 22-46), and closed out (row 47). `./led show 42` (one of the three anchor rows the build
# task named) sits INSIDE this pipeline -- it is the work_claimed row for the third phase,
# msched-witness-schedule -- not a compound pipeline in its own right; see
# design/workflows/README.md "Known misfits" for why the anchor-row citation needed this
# transcription-time correction.
#
# Read literally: this pipeline runs ONCE, all three phases converge on their first review pass
# (rows 29-31 all countersign without a REJECT), and every acceptance-criteria check (rows 23,
# 37, 43) reports PASS on first witness (rows 32/34, 39, 44). The escalation_event fields below
# name the typed event this shape WOULD route to if a phase's review or verification did not
# converge -- not something this specific run exercised, since it converged cleanly throughout.

[[phases]]
name = "provision"
depends_on = []

[[phases]]
name = "declare-resource"
depends_on = ["provision"]

[[phases]]
name = "witness-schedule"
depends_on = ["provision", "declare-resource"]

[roles.provision]
authors = "sonnet-main-session"
implements = "sonnet-main-session"
reviews = "sonnet-independent-subagent"

[roles.declare-resource]
authors = "sonnet-main-session"
implements = "sonnet-main-session"
reviews = "sonnet-independent-subagent"

[roles.witness-schedule]
authors = "sonnet-main-session"
implements = "sonnet-main-session"
reviews = "sonnet-independent-subagent"

[convergence.provision]
done = "acceptance criteria (row:23) all PASS: sibling checkout present, no submodule/gitlink, editable-installed into the shared venv, import+call succeeds, this repo's own git status unaffected -- verified in row:32/34."
escalation_event = "non-converging-review-loop -- if the independent reviewer's countersign (row:29) had found a real defect (not just an unantecedented-but-reasonable assumption), it routes to the maintainer rather than the implementer self-adjudicating."

[convergence.declare-resource]
done = "acceptance criteria (row:37) all PASS: ledger resource: decision row with all six fields at blessed tier, honest GUIDANCE field, CLAUDE.md RESOURCES section mirrors it, ./pickup surfaces it -- verified in row:39."
escalation_event = "non-converging-review-loop -- as above, routed via the independent reviewer's countersign (row:30)."

[convergence.witness-schedule]
done = "acceptance criteria (row:43) all PASS: jobs.json models the real decomposition against a genuine shared resource, the tool is actually invoked, the result is ledgered honestly noting agreement/disagreement with the real work_depends precedence, the schedule artifact is saved and cited -- verified in row:44."
escalation_event = "non-converging-review-loop -- as above, routed via the independent reviewer's countersign (row:31)."

[landing_zones.provision]
zone = "sibling checkout /home/bork/w/vdc/1/experience/makespan-scheduler (editable-installed into ~/w/vdc/venvs/generic); ledger rows 15/22/32/34/35."

[landing_zones.declare-resource]
zone = "ledger decision row 38 (resource: ... blessed tier) plus this repo's own CLAUDE.md RESOURCES section; ledger rows 16/36/37/39/40."

[landing_zones.witness-schedule]
zone = "scratch/msched-witness/schedule.json, committed to the panel repo as cited evidence; ledger rows 17/42/43/44/46."
