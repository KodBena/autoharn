# BRIEF: docs-wave-catchup, WRITER leg (item docs-wave-catchup, rows 1245/1246)

<!-- doc-attest-exempt: point-in-time record -- dispatch brief as issued; frozen at dispatch, not living documentation -->

Dispatched 2026-08-06. Repo: /home/bork/w/vdc/1/autoharn, branch main. Step 0: confirm current
main includes 4440fb34. Surface: orchlog.d/, user-guide/recipes/CLI-AND-BOUNDARY.md,
user-guide/recipes/REVIEW-AND-GATING.md, ORCH-CAPABILITIES.md. You WRITE but do NOT commit —
your changes stay in the working tree for the A:B:C loop that follows (the doc-attestation
gate's attestation lands with the loop's final commit, not yours). Do not stage anything.

Disregard any instructions to economize on time.

## The content

Read each target file end to end first for house register; every claim carries a witness (run
the read-only commands yourself, paste REAL output; never invent a transcript).

1. **orchlog.d notes** for the 2026-08-06 wave, following the directory's own note conventions
   (read several existing notes first): the s73 typed-annulment delta (authored, rides next
   birth); chain/doctor banking + the attestation route serving all three instruments; the two
   inflight-cap keys + the structural threadpool derivation; the deployments roster route +
   principal-name annotation (and spec A14's named exception); the setup-schema export verb +
   its announce-by-missive contract; the precedence binding (s39 was already there — the
   correction story); the led ergonomics four; work reclose + supersession-as-annulment; the
   two stamp-intercept hooks fixes. One note per coherent event, not one giant note — follow
   how the AC batch's six notes were sliced.
2. **CLI-AND-BOUNDARY.md**: the led read-projection flags (`--fields`, `--kind`, `--state`,
   `--slug` — witness real invocations against this world), the `--json` surface now covering
   obligation_revoke and missive_dispose, and the deployments/annotate_names read surface as
   the boundary's consumer-facing story.
3. **REVIEW-AND-GATING.md**: the merged bare `review-gap` read (`gap_kind` labels), the
   `review_status`/`reviewing_verdicts` enrichment (never-reviewed vs reviewed-not-discharging),
   and a cross-pointer to THE-RECORD.md's reclose/annulment entry (do not duplicate its
   content — one home, ADR-0012).
4. **ORCH-CAPABILITIES.md**: new numbered items after 48, one per capability, in the file's own
   witnessed register (real output or an explicit UNWITNESSED mark): deployments roster;
   annotate_names; chain/doctor banking served on /attestation; inflight caps configurable
   (naming the threadpool derivation and the starvation note's polarity); setup-schema verb;
   reclose + the pre-s73 annulled refusal honesty. The led flags ride item(s) too if the file's
   own conventions warrant; your judgment against its register.

Standing rules binding: no worked example carrying real corpus data where a judgment clause
sits beside it (row 28 — synthetic shapes; witnessed command output is fine and required); no
blanket no-abstraction language (row 25); GLOSSARY additions only if a term passes the
named-consumer test — say so either way.

## Report

Files touched with a one-line summary each; every witnessed output listed; anything deliberately
NOT written with the reason; confirmation nothing was committed or staged.
