# BRIEF: typed-annulment kernel delta (item review-obligation-annulment-vocabulary part (c), spec ratified row 1087)

<!-- doc-attest-exempt: point-in-time record -- dispatch brief as issued (rows 1087); frozen at dispatch, not living documentation -->

Dispatched 2026-08-06. Repo: /home/bork/w/vdc/1/autoharn, branch main. Confirm current main
first. Another builder works serving/ + libexec/autoharn/{verify-chain,doctor} concurrently —
your surface is kernel/lineage/, the LINEAGE_CHAIN wiring in bootstrap/, and whatever
fixture/gate files the house conventions mechanically require for a new lineage file (the
fixture-census and kind-shape gates will tell you). Nothing else. If you believe you must edit
outside it, STOP and report. Before committing, `git pull --rebase` and retry once on index
contention; your commit stages only your own files (CLAUDE_COMMIT_PATHS per the staging guard).

Disregard any instructions to economize on time.

## Commission

Implement design/FABLE-TYPED-ANNULMENT-DISPOSITION-SPEC.md (maintainer-ratified 2026-08-06,
ledger row 1087) — read it FIRST and in full; it is the binding text and this brief is only its
dispatch wrapper. Then read, in full, before designing: law/adr/ ADR-0000, ADR-0008, ADR-0012;
kernel/lineage/s29-obligation-item-key-and-typed-close.sql (the disposition vocabulary you
widen), s31-supersession-uniform-retraction.sql (the annulment-in-substance path you compose
with), s21-session-aware-distinctness.sql and s40/s41 (the identity substrate your
no-self-annulment predicate designs against), and s39-blocks-start.sql END TO END — it is the
freshest idiomatic precedent for everything this delta must carry: HISTORY-safe re-issue-wider
CHECK, additive-only re-issues with byte-identical pre-existing branches, detect sibling
fingerprinting BEHAVIOR not object names, ENGINE section verified-not-asserted, LIMITS
pre-registered, fail-safe classification stated honestly (yours is NOT class-ratified — the
spec's §3 says why; the maintainer's ratification row 1087 is your authority instead, cite it).
Standing row 26 (no bare types) binds; read it via `./autoharn led standing`.

## Scope

1. One new lineage file at the next free sNN (head is currently s72 — verify, don't assume),
   name it for what it does, implementing the spec's §2 in full: the third
   work_review_disposition value 'annulled'; the three construction-time refusals (mandatory
   in-force authority citation via work_review_ref; no self-annulment with the distinctness
   predicate YOU design against s21/s40/s41 and defend in the header; legal only where
   'deferred' would be); work_review_gap treating an in-force annulled close as discharged;
   the new single-home audit view over annulled closes; the defense-in-depth judgment call the
   spec leaves to you, stated either way in LIMITS.
2. Detect sibling per house convention.
3. LINEAGE_CHAIN wired in the SAME commit (s37/s38/s39 precedent) — find every chain home the
   gates enforce (bootstrap/new-project.sh at minimum; the kind-shape/fixture gates and
   gates/kernel docs will surface any others).
4. Scratch witness, BOTH polarities, on throwaway schemas in the toy db per the lineage files'
   own PARAMETERIZATION idiom: accepted annulled close with valid distinct-actor authority;
   refused on missing ref, dangling/retracted ref, self-annulment, and non-debt context; the
   SQL/ASP differential (./judge) in AGREE, per the spec's §2 ENGINE clause — verified, not
   asserted. NEVER applied to any live world (runs are linear; it rides the next birth).
5. Commit citing ledger rows 1025/1087, Co-Authored-By line for your model.

Out of scope: the B2 surface verb (follows separately), serving/, libexec/, law/, hooks/,
tools/, any edit to an existing frozen sNN file beyond the CREATE OR REPLACE re-issue idiom
your own delta performs.

## Report

Per item: WITNESSED with verbatim observed output (both polarities), REFUSED-AS-EXPECTED, or
UNEXERCISED with the concrete blocker — no umbrella claims. Name the distinctness predicate you
chose and why; name the defense-in-depth call you made and why; flag (don't fix) anything found
in reach, unless it is a hazard per CLAUDE.md's corollary.
