% core-a-broken.lp — the deliberately BROKEN program fixture for run-core-a.sh --negative-control
% (engine INC 1 accept; ruling 110 §5 rider on refute-evaluation flaw 1(b)). A syntax error here
% must surface as a LOUD ERROR in the sweep — under the pre-fix runner (stderr silenced, substring
% grep) this file produced an empty line that read as a clean phase. Do not "fix" this file: its
% brokenness is its function.
phase(1..k
broken_rule :- this line does not parse.
