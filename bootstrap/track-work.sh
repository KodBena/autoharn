#!/bin/sh
echo "bootstrap/track-work.sh: RETIRED 2026-07-25 (ledger row 1271, design/" >&2
echo "  FABLE-TRACK-WORK-RETIREMENT-SPEC.md) -- this script's own STANDING vs WORLD offering is" >&2
echo "  now a mode on the ONE normal scaffold, modernized (full current kernel lineage, not the" >&2
echo "  s25 cap this script carried; boundary served via ensure-running, auto-spawned on first" >&2
echo "  ./led use -- no standing daemon to stand up by hand). Use this instead:" >&2
echo "" >&2
echo "  bootstrap/new-project.sh <project-dir> --profile tracker --name <name> --db <db> \\" >&2
echo "      --host <host> [--schema <schema>] [--kern <kern>] [--role <role>] [--force]" >&2
echo "" >&2
echo "  'a standing project is not a governed world' still holds, in this script's own words," >&2
echo "  preserved: --profile tracker wires NO hooks and NO governance CLAUDE.md preamble, same" >&2
echo "  as this script always did. Git history keeps this script's original body (git log -p" >&2
echo "  --follow bootstrap/track-work.sh); user-guide/TRACK-WORK-RETIREMENT-HERITAGE.md records" >&2
echo "  what it was, why its serviceless shape retired, and the exact modern invocation." >&2
exit 1
