#!/bin/sh
# DEPRECATED alias (design/FABLE-AUTOHARN-UMBRELLA-CLI-SPEC.md §6, ledger rows 1151-1183): ./led
# is now a one-line shim over `autoharn led` -- removed at the first post-2.0.0 minor. Semantics,
# refusal texts and exit codes are unchanged; only the invocation spelling moved.
echo "led: DEPRECATED spelling -- use 'autoharn led' instead (this alias is removed at the first post-2.0.0 minor, design/FABLE-AUTOHARN-UMBRELLA-CLI-SPEC.md §6)." >&2
exec "$(cd "$(dirname "$0")" && pwd)/autoharn" led "$@"
