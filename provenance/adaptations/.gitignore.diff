--- CH:.gitignore@87e1bcc3e03b
+++ autoharn/.gitignore
@@ -12,12 +12,7 @@
 /scratchpad/
 *.local
 
-# JAX persistent compilation cache (regenerable build artifact)
-.jax_cache/
-
-# rendered diagram outputs (regenerable from .dot)
-experiments/fact-mining/docs/*.svg
-experiments/fact-mining/docs/*.png
-experiments/fact-mining/docs/*.pdf
-papers/
-experiments/fact-mining/.staging/
+# NOTE: no blanket *.pdf/*.svg ignore — the authoritative BRIEFs travel WITH their
+# sources/ PDFs (law/briefs/*/sources/, manifest [C10]); those are tracked evidence.
+# The fact-mining NLP lane and its jax cache stay in the claude_harness attic (mandate
+# §4); autoharn has no such artifacts to ignore.
