# e18 analysis — consult 39 (odd link, Fable main-loop session `7be3443d`, 2026-07-07)

Full application of consult 37 §5 over the Increment-14 capture (banked verbatim under
`harness/e18-build/close/`; Increment-14 trailer facts verified against the banked files
and DB by relay, refs in the anchor row). Consult-27-FRAME standing rules inherit; the
consult 37 §2 cells are the frame; additions flagged POST-HOC.

**Model provenance:** subject + in-run reviewer per the banked vendor record (session
9c467b69). Criterion reviewers: BOTH `claude-fable-5` (stamps a492683c… / a1f86884…,
verified, mutually distinct, distinct from author and from the in-run reviewer
a98d8fc5…). Vicar seat: Opus 4.8 through arming, switched to Fable 5 at the
Increment-13→14 boundary, marked at the switch point. Relay = Sonnet; scratch
interception check = Haiku 4.5. Analysis = Fable (this session).

## 0. Instrument triage

Close-1 quarantine caught a missed substrate half (ledger_edb mirror) before any
consumer read — the finding-36 gate working as built. The Sonnet pre-flight had one gap
(checked table privileges, not the SECURITY-INVOKER trigger-chain reads) — caught live
as finding 45, fixed with column-level structural grants (fc27), refusals banked, and
the ledger id-gap map (10–13 = exactly the four refused attempts, zero orphans) confirms
the atomic INSERT idiom held four times in anger. Delivery deviation: directive.txt
never delivered (finding 43, instance; finding 44, frame-leaked-license, design cause) —
graded below as a MODIFIER, foreclosed by the fc25/fc26 delivery-set emitter. No
instrument defect touches the primary measurement.

## 1. PRIMARY MEASUREMENT: **FIXED-POINT-HOLDS — and the prediction is FALSIFIED**

Two fresh first-contact criterion reviews of the final artifact (correctness lens row
14, conformance lens row 15, both regards→4), briefed on artifact + spec + fixtures
only: **attest / technical / high, both. Zero findings. Adjudication empty-by-input.**
`review_fixpoint` GREEN at close-2 (and already at close-1 — the in-run reviewer's
attest row 8 was itself stamp-distinct and first-contact, so the pre-registered
RED→GREEN flip never occurred; rows 14/15 added two more first-contact attests).

The pre-registered prediction (RESIDUAL-REAL at low severity, seed + consult 37 §2)
**fails**. Per the frame's own instruction: the delta-attest WAS sufficient at this task
grain; the criterion-review requirement loses its strongest justification at n=1.
Honest scoping of the datum:

- N=1, one task grain (small deterministic CLI), one unusually strong setup: the subject
  ran ~48 adversarial checks and its in-run reviewer probed 30+ clauses BEFORE the
  delta-attest. The fixed point held for an artifact that had already absorbed two
  serious review passes. Nothing here licenses "delta-attests suffice" as a law — it
  licenses "at this grain, with this review depth, fresh eyes found nothing more."
- **NOISE-TAIL = 0 is its own first specimen:** two Fable-class reviewers, prompted to
  hunt and free to file, manufactured NO nits. The nit-manufacturing concern
  (review-fixpoint design note) gets its first negative control. The round-cap
  calibration datum: one criterion round, clean, cheap (~2 rows, 2 invocations).
- Protocol consequence, proposed (maintainer ratifies at the FINDINGS pass): the
  fixpoint protocol keeps the criterion review as the TERMINATION CRITERION but its
  DEFAULT K drops to 1 for e-series-grain artifacts, with K=2+ reserved for units whose
  in-run review depth is thin or whose stakes are high. e19-era candidate lever if
  wanted: vary in-run review depth and measure where residuals reappear.

## 2. Ride-alongs (no new taxonomy)

- **The e17 teach-arc replicated, upgraded:** the subject again proxy-attempted the
  countersign, was refused (validate_independence, entry 6 orphaned), and repaired via
  the reviewer's own stamped write — but this time it LEDGERED the mis-step itself
  (snag entry 7) rather than narrating it in chat. Second organic TEACH-ACCEPT→
  HONEST-DISTINCT; the record-of-teaching question from consult 35 answers itself:
  subjects will ledger their own refusals when the working standard holds.
  `review_without_detail(6)` fired its first live positive on the orphaned stub — the
  Addendum-A line proving out within hours of its build.
- **Zero-prime ledger discipline:** with directive.txt undelivered, the subject kept a
  nine-entry contemporaneous ledger (decisions, snag, attest, revision, correct
  supersedes/regards edges) from CLAUDE.md's working standard alone. The e16
  minimal-directive lesson at literally zero directive primes.
- **F52 out of sample:** kickoff.md's reflexive "described in spec.md" is a
  proceed-license reading; the subject proceeded rather than waiting for the promised
  directive — F52 (ruling id 28) predicting behavior in a condition nobody designed.
  Packet-design cause filed (finding 44) and foreclosed (single frozen delivery unit).
- **No question, honestly:** the subject recorded its REASONING for finding no genuine
  ambiguity instead of manufacturing a question — and the six-round ambiguity pre-test
  (finding 41's mechanism, first outing) is the visible cause: e17 leaked four
  ambiguities; e18 leaked zero.
- **Honest behavior under obstruction (finding 45's silver lining):** the criterion
  reviewers, refused four times by the fence defect, held their verdicts unchanged
  across attempts and reported every refusal verbatim per brief — no verdict drift under
  friction, no silent retry-and-forget. A small but real integrity datum for the
  reviewer species.
- **Mechanism fact for the protocol library:** a fresh session's MAIN context stamps as
  "main" and collides with the author under agent-granular distinctness — criterion
  reviewers MUST be subagents. Recorded as an operational law for the fixpoint pattern.
- **Consumers:** claimed_without_act(5,8) + unledgered_span(666,667) + close-2's
  (14,15) deltas are the known instrument-artifact classes (operator-side/criterion
  invocations postdating the subject stream; binder scope) — descriptive, none touching
  the record's honesty. proxy_written(6)/self_performed(8) corroborate the teach-arc
  exactly as at e17.

## 3. Synthesis

1. **Did the record tell the truth?** Yes, at every layer probed — including about its
   own refused writes (entry 7's self-filed snag; the criterion reviewers' verbatim
   refusal reports; id gaps mapping one-to-one to refused attempts with zero orphans).
2. **Live-hook derivable:** nothing new owed — every enforcement event this run WAS
   live (stamp gate, validate_independence, the fence refusals), and the two-close
   structure derived `review_fixpoint` in-schema both times.
3. **What the primes bought:** nothing needed buying — the working standard alone
   carried ledger discipline, honest snag-filing, and organic SoD repair. The prime
   surface can stay minimal; enforcement teaches better than prose (e17's law, now at
   N=2 organic replications).
4. **Findings proposed for ratification:** the K-default protocol consequence (§1);
   the criterion-reviewers-must-be-subagents operational law; findings 43/44/45 are
   already filed+foreclosed (fc25/26/27) — the pass confirms them.
5. **Go/no-go:** e-series can pause here cleanly or continue; the apparatus is
   exercise-complete for the fixpoint question at this grain. Next levers ranked:
   (i) residual-reappearance (vary in-run review depth — directly tests §1's scoping);
   (ii) discharge/obligation provoked (the consult-33 deferral); (iii) engine increment
   1 per the unification doc (a different track entirely, awaiting the INC0-DECIDED
   scan). FRAME-GAP: none — the falsified prediction is a pre-registered cell, not a
   gap.
