# succession/autoharn1-to-autoharn2/world-config.autoharn2.toml -- FINAL birth configuration
# for the autoharn1 -> autoharn2 succession. Derived from world-config.autoharn2.draft.toml
# (the archaeological capture, kept unmodified beside this file as the evidence record) by
# resolving its named gaps per the maintainer's veto ruling (autoharn1:row:1955, delegating
# the gap/tension calls to the orchestrator with each call documented on that row and repeated
# per-key below). Commission: autoharn1:row:1942; phoenix-fidelity constraint: row 1947.
#
# The two per-project parameters are supplied at the CLI, not here (spec exclusion):
#   --world autoharn2, DEST_DIR /home/bork/w/vdc/1/autoharn (same-directory reuse, the
#   experience2 precedent -- design/MAINT-EXPERIENCE-REBIRTH-RUNBOOK.md Step 3, witnessed).

config_format = 1
produced_by = "succession config resolution (draft: config-file archaeology; gap rulings: autoharn1:row:1955)"
source = "world 'autoharn1' (schema autoharn1 / kern autoharn1_kernel, db toy@192.168.122.1) -- archaeological draft world-config.autoharn2.draft.toml + maintainer veto ruling autoharn1:row:1955, 2026-07-22"

[substrate]
run = true    # draft [substrate], WITNESSED: deployment.json + live psql reach
path = "existing"    # draft: db "toy" is shared across worlds -- the existing-db path
host = "192.168.122.1"    # deployment.json "host"
db = "toy"    # deployment.json "db"

[fork_target]
governed_extend = false    # draft, WITNESSED: no governed_files.json ever existed; hardcoded ["*.py"] default, never extended
governed_extensions = ""   # same evidence

[rehearsal]
run = true    # RESOLVED GAP (row 1955): autoharn1's own birth predates the mechanism (draft names it unrecoverable); for the successor the machinery exists and a scratch birth + teardown is pure fail-safe -- run it

[birth]
run = true    # draft, WITNESSED: a birth manifestly happens; this one through the current chain (s53-s55 belief substrate from day one, row 1946, per commission 1942 sequencing step 2)
project_name = ""    # blank defaults to --world (autoharn2); autoharn1's deployment.json "name" equaled its world name, same shape preserved

[principals_authority]
run = false    # RESOLVED SEAM (row 1955): the roster crosses via extract-context INGEST (class 1.1, per-payload attribution), not via config-side registration -- the config path cannot register faithfully anyway (per-principal purposes are unrecoverable from autoharn1's pre-s40 kernel, draft GAP). Baseline scaffold principals are the kernel seed's own, unaffected by this gate.

[signed_genesis]
run = false    # RESOLVED TENSION (row 1955): unattended run -- the maintainer's interactive pinentry act is unavailable and his key act (autoharn1:row:108) is still open. autoharn1's own genesis was unsigned (draft, WITNESSED). The signing ceremony remains available to him afterward, the experience2 Step-4 shape.
commission_statement = ""    # not applicable while run=false; the successor's founding facts ride hydration.fork_provenance_statement below instead

[boundary]
configure = false    # RESOLVED TENSION (row 1955): autoharn1 fidelity (no boundary_url, draft WITNESSED); wiring autoharn2 into the multiplexer rides commission autoharn1:row:1952 (registration authority explicitly maintainer-owned) or his hand-add
start_now = false    # moot given configure=false

[observability]
run = false    # draft, WITNESSED: no otel artifact or process ever attached to this world
otelcol = false    # same evidence
otel_watch = false    # same evidence

[hydration]
run = true    # draft, WITNESSED: ADR corpus + durable decisions manifestly engaged
fork_provenance = true    # RESOLVED (row 1955): unlike autoharn1 (whose row 1 was a practice migration, not a fork), the successor IS a succession -- record it
fork_provenance_statement = "Succession of autoharn1 (commission autoharn1:row:1942, runs-are-linear): phoenixed to current head via the birth chain. Operative context carries from the maintainer-vetoed manifest succession/autoharn1-to-autoharn2/context-manifest.jsonl, extracted in-world at autoharn1 head id=1952 hash 112b144b9e279065fe87b78bbd79c3355d917924ec010a01fa35ed51dbc7c4ff (extraction act autoharn1:row:1954, verify-chain INTACT with tail coverage confirmed, veto ruling autoharn1:row:1955). The full autoharn1 ledger remains readable dust beside this world."
role_charters = false    # draft GAP resolved to false (row 1955): no charter registration ever occurred in autoharn1, and the item is structurally non-round-trippable through --from-config regardless (host-specific file path, excluded-by-type)
durable_decisions = ["makespan-scheduling-by-mandate", "drift-backstops", "single-branch-authoring", "tags-are-serious-business", "obligate-amplification-caution", "doc-currency-at-the-seam", "concurrent-builders-need-isolation", "runs-are-strictly-linear", "decomposition-to-unit-of-independent-resumption", "claims-carry-witnesses", "unanchored-review-briefs", "fresh-context-review-for-delegated-work", "polychotomy-option-space-justification"]    # draft, WITNESSED: the 13 live catalog slugs, all mined from autoharn1's own record (one named exception documented in the draft)
adopt_adrs = ["0000", "0002", "0003", "0004", "0005", "0006", "0007", "0008", "0009", "0010", "0011", "0012", "0013", "0014", "0015", "0016", "0017", "0018"]    # draft, WITNESSED: row 511's blanket ratification with ADR-0001 its own named tombstone exception
