# >>> PROVENANCE-STAMP >>> (auto; tools/hooks/stamp_provenance.py — do not hand-edit)
#   first-seen : 2026-07-22T03:18:21Z
#   last-change: 2026-07-22T03:18:37Z
#   contributors: 431cddfa/main
# <<< PROVENANCE-STAMP <<<

# succession/autoharn1-to-autoharn2/world-config.autoharn2.draft.toml -- DRAFT birth
# configuration for the autoharn1 -> autoharn2 succession (ledger row 1942/1947 commission;
# config-file feature and FORMAT authority per design/FABLE-SETUP-TUI-CONFIG-FILE-SPEC.md,
# merged row 1948; schema read from tools/setup_tui/content/config_file_data.py, ledger read
# from autoharn1/autoharn1_kernel on 192.168.122.1/toy per deployment.json).
#
# METHOD (maintainer-directed, row 1947, verbatim): the blank-world known-good template
# (bootstrap/templates/known-good-blank.toml) is used HERE ONLY as a FORMAT/vocabulary
# exemplar -- its section/key shapes and its per-key trailing-evidence-comment convention.
# NOT ONE VALUE below is copied from that template or from any default. Every value traces to
# autoharn1's own evidence, cited in its own trailing comment; every value that could not be
# recovered faithfully is a NAMED GAP block instead of a guess.
#
# THIS DRAFT IS NOT SELF-APPLICATION-READY as a --from-config file: several REQUIRED_GATES
# scalars are deliberately left as named gaps rather than fabricated, so tools/setup_tui/
# config_file.py's own require_complete=True validation would legitimately refuse it. That
# refusal is correct and intended -- this file's job is archaeological fidelity + gap-naming
# for the maintainer, not an unattended birth.
#
# SEAMS NOTED, NOT RESOLVED HERE (maintainer decisions per the commission):
#   (1) succession/autoharn1-to-autoharn2/context-manifest.jsonl (class 1.1_principal_roster,
#       156 rows, row-span 1-1950, chain_head_id 1950) ALSO carries the full principal roster
#       (id/name/agent_class per principal, disposition "carry-reopened", no purpose field
#       either -- same absence this file finds below). Registering the roster again via this
#       birth-config's [[principals_authority.register]] AND ingesting the manifest would
#       double-register the same seven identities. Both mechanisms are left in place; the
#       maintainer picks which one (or both, deliberately) drives autoharn2's actual roster.
#   (2) The successor's NAME and destination directory are the two per-project --world/--dest
#       parameters supplied at birth (design/FABLE-SETUP-TUI-CONFIG-FILE-SPEC.md spec 1's own
#       exclusion), never config keys -- they do not appear anywhere below, by construction.
#
# GENESIS / FOUNDING RECORD, VERBATIM (autoharn1.ledger id=1, ts 2026-07-11 22:42:12+02,
# actor=1 'author', kind=decision -- NOT kind=commission; this world's ledger-tracker founding
# act was never GPG-signed, since the signed-genesis ceremony (row 1726, merged 2026-07-19)
# postdates this world's own birth by 8 days):
#
#   "This store (schema=autoharn, kern=autoharn_kernel, db=toy@192.168.122.1) is the work
#   tracker for the autoharn project itself, effective 2026-07-11 (design/WORK-STATUS-
#   OFFERING.md, closing the 3x-litigated omega work-status question as a product). BACKLOG.md
#   is henceforth the findings JOURNAL -- dated rulings, findings, and dispositions -- never
#   the work TRACKER; open/queued work is tracked here via led work open/claim/close, read via
#   ./pickup and ./distance-to-clean. The migrated inventory that follows cites HANDOFF.md's
#   'Open work' section and BACKLOG.md's dated tail as of this migration; rejected: continuing
#   to abuse BACKLOG.md as a work tracker (the maintainer's own diagnosis, this commission's
#   framing) because a 2k+ line append-only prose file cannot answer 'what is open, what is
#   blocked, what is done' without a human re-reading it end to end."
#
#   NOTE ON THE SCHEMA NAME IN THAT ROW: row 1 self-describes as "schema=autoharn,
#   kern=autoharn_kernel" -- the CURRENT deployment.json says schema=autoharn1/kern=
#   autoharn1_kernel. autoharn1.ledger row 776 (finding, 2026-07-15) independently confirms a
#   schema rename occurred ("stale schema left in the toy db from before a rename"). This
#   config cites deployment.json's CURRENT values throughout; the rename itself is carried here
#   only as this note, not resolved (not this document's job).
#
# CHAIN STATE AT CAPTURE (autoharn1_kernel.chain_genesis / chain_high_water, live query
# 2026-07-22): genesis seed 3e67e364eebf20653fcbe00ad8f0e0d24036bee3b539aa331aaa5e57bb8e6a2c
# (seeded_at 2026-07-15 17:31:38+02 -- NOT this world's original birth instant; row 816/817
# record that the hash-chain genesis-seed mechanism (kernel/lineage/s26) was retrofitted and
# this world was re-cut through it during a 2026-07-15 self-migration, epoch 1058 per
# autoharn1_kernel.migration_epoch), chain_high_water.max_id = 1950, ledger row count = 1888
# (row 1888 < max_id 1950: the ledger is append-only but the id sequence also advances on rows
# that were never durably committed to this table, e.g. refused/rolled-back inserts -- a gap
# between count and max id is expected under that append-only-but-sequence-advances shape, not
# itself an anomaly this file investigates further).

config_format = 1
produced_by = "succession config-file archaeology (design/FABLE-SETUP-TUI-CONFIG-FILE-SPEC.md; ledger rows 1942/1947/1948)"
source = "world 'autoharn1' (schema autoharn1 / kern autoharn1_kernel, db toy@192.168.122.1) -- live read-only archaeology, 2026-07-22, against deployment.json + autoharn1.ledger (rows 1-1950) + autoharn1_kernel.* + law/adr/ + disk artifacts"

# =============================================================================================
# [substrate] -- WITNESSED
# =============================================================================================
[substrate]
run = true    # deployment.json exists at repo root and names a live substrate; WITNESSED live: `psql -h 192.168.122.1 -U autoharn_rw -d toy` reaches schema autoharn1 / kern autoharn1_kernel (queried directly for this capture)
path = "existing"    # deployment.json's db "toy" is SHARED: the same db/host also carries omega/omega1, ent, experience, and other worlds' schemas (seen live via sibling boundary_service processes and cross-referenced ledger rows citing those schemas) -- this is the existing-db path, not a dedicated one
host = "192.168.122.1"    # deployment.json "host"
db = "toy"    # deployment.json "db"
# substrate.role intentionally OMITTED: SCHEMA only requires it on the "dedicated" path (config_file.py's own _missing_contextual). NOTE (not a gap, a flagged fact): deployment.json "role" = "autoharn_rw" while "schema"/"kern" = "autoharn1"/"autoharn1_kernel" -- the role name did NOT track the schema's own rename (see the header note above on the autoharn->autoharn1 rename, row 776). Recorded here as evidence about this world's own naming history, not fixed.

# =============================================================================================
# [fork_target] -- WITNESSED
# =============================================================================================
[fork_target]
governed_extend = false    # no `.claude/governed_files.json` exists at repo root (checked live; only per-agent worktree copies of the bootstrap TEMPLATE exist, which are not this world's own config) -- hooks/pretooluse_change_gate.py's own `_load_governed_patterns` falls through to its hardcoded `_DEFAULT_GOVERNED_PATTERNS = ["*.py"]` for this repo (its own comment names this "autoharn's own bare hardcoded-default usage"); no extension was ever configured
governed_extensions = ""    # same evidence -- nothing extended, and the governed-files EXPOSURE screen itself (commission row 1730/1731, 2026-07-19) postdates this world's birth, so "operator declined" is not quite the right frame either: this world's governance was never offered the choice, it simply runs the hardcoded default

# =============================================================================================
# [rehearsal] -- GAP-NAMED
# =============================================================================================
# GAP: no rehearsal (scratch birth + teardown + zero-residue check) artifact exists for
# autoharn1's OWN birth. The rehearsal screen/mechanism is TUI-era tooling (design/FABLE-
# SETUP-TUI-SPEC.md family, built 2026-07-19 onward) that postdates this world's 2026-07-11
# founding by over a week; there is no ledger row, checklist, or journal in this world's own
# record that a rehearsal preceded its birth (its birth predates the concept). Recording
# `run = false` would misrepresent "rehearsal was offered and declined" as fact, which is not
# what happened -- naming the gap instead of guessing either polarity.
[rehearsal]
# run = <GAP: unrecoverable -- mechanism postdates this world's birth, see comment above>

# =============================================================================================
# [birth] -- WITNESSED
# =============================================================================================
[birth]
run = true    # WITNESSED live: schema autoharn1 + kernel schema autoharn1_kernel both exist and are populated (autoharn1_kernel.chain_genesis has exactly one row; autoharn1.ledger holds 1888 rows spanning id 1-1950) -- a birth manifestly happened, even though it predates the TUI's own [birth] screen and its journal
project_name = "autoharn1"    # deployment.json "name" -- happens to equal the world/schema name here; recorded because it is a real, present deployment.json key, not because it differs from --world

# =============================================================================================
# [principals_authority] -- WITNESSED (roster) / GAP-NAMED (purpose, competences, relations)
# =============================================================================================
# WITNESSED roster (live query, autoharn1_kernel.principal, 2026-07-22): exactly 7 rows.
#   id=1 name=author                  class=model     acts_for=NULL
#   id= 2 name=reviewer                class=subagent  acts_for=NULL
#   id= 3 name=commissioner            class=human      acts_for=NULL
#   id= 4 name=maintainer              class=human      acts_for=NULL
#   id=21 name=bork                    class=human      acts_for=NULL
#   id=22 name=countersign-reviewer-01 class=subagent  acts_for=NULL
#   id=23 name=otel-sentry             class=tool       acts_for=NULL
# (also independently corroborated by succession/autoharn1-to-autoharn2/context-manifest.jsonl's
# own class "1.1_principal_roster" rows, disposition "carry-reopened", same 7 identities -- see
# the double-registration seam noted in this file's header.)
#
# GAP, all seven principals, PURPOSE: autoharn1_kernel.principal carries only (id, name,
# agent_class, acts_for) -- there is NO purpose column, and none of the kernel deltas that add
# one (s40-principal-identity-events.sql onward: principal_registered events with a stated
# purpose, per design/FABLE-PRINCIPAL-IDENTITY-SPEC.md) have been applied to autoharn1_kernel
# (WITNESSED: autoharn1_kernel has exactly 6 tables -- chain_genesis, chain_high_water,
# migration_epoch, principal, principal_role, stamp_secret; no principal_registered/competence/
# relation event tables exist; autoharn1_kernel.migration_epoch.epoch = 1058, applied
# 2026-07-15, well before the s40/s41 family (row 1448, 2026-07-18) even shipped anywhere). No
# ledger prose states a formal per-principal purpose for any of the 7 either (searched: no
# 'register-principal'/'grant-competence' text at all in this ledger's 1888 rows -- the ledger
# `kind` CHECK constraint itself has never had a principal-registration kind). Corroborating,
# not contradicting: actor-usage counts across all 1888 rows are author=1882, commissioner=1,
# maintainer=6, and ZERO for reviewer/bork/countersign-reviewer-01/otel-sentry (live GROUP BY
# on autoharn1.ledger.actor) -- i.e. 4 of the 7 registered principals have never once acted in
# this world's own ledger, so even an inferred-from-usage purpose is not available for them,
# and for the 3 that have acted, no row states WHY beyond the generic role name itself. Writing
# a purpose string for any of the 7 would be inventing content this world never recorded --
# per the method rule, that is named as a gap, not filled from the omega exemplar's shape
# (which had a purpose column to read from) or from FABLE-PRINCIPAL-IDENTITY-SPEC.md's generic
# role descriptions (those describe what a role like "commissioner" is FOR IN GENERAL, not what
# THIS world recorded for THIS principal).
#
# GAP, competences (activity/band/basis): same root cause -- no kernel column, no ledger event
# kind, no ledger prose naming an activity/band/basis for any of the 7.
#
# GAP, relations: autoharn1_kernel.principal.acts_for is NULL for all 7 rows (no acts-for edges
# ever recorded structurally); ledger full-text search for "acts-for" across all 1888 rows
# returns exactly one hit (row 1884, an unrelated AC-1-artifact discussion, not a relation
# assertion). No relation exists to record.
[principals_authority]
run = true    # the roster above demonstrably exists and 3 of 7 principals have acted -- principals-and-authority is manifestly engaged for this world, even though the roster predates the principals-authority SCREEN (commission row 1727, 2026-07-19) by 8 days
# register = <GAP: name/agent_class witnessed above (7 rows); purpose unrecoverable for all 7, see comment above -- omitted rather than fabricated>
# competences = <GAP: no activity/band/basis recorded for any principal, see comment above>
# relations = <GAP: no acts-for relation recorded for any principal, see comment above>

# =============================================================================================
# [signed_genesis] -- WITNESSED (as declined-by-nonexistence)
# =============================================================================================
[signed_genesis]
run = false    # the signed-genesis ceremony (GPG-signed founding commission) was built 2026-07-19 (row 1726, merged 14ffb57) -- 8 days after this world's own 2026-07-11 founding (ledger row 1, kind=decision, unsigned, no GPG artifact). No keys/ directory, no signed-commission file, and no ledger row of kind='commission' for this world's own founding exist (row 1 is kind=decision). This is a real, evidenced "no" (the mechanism did not exist yet), not a guessed default.
commission_statement = ""    # not applicable -- see run=false above; the closest analogue is the unsigned genesis statement quoted verbatim in this file's header

# =============================================================================================
# [boundary] -- WITNESSED
# =============================================================================================
[boundary]
configure = false    # deployment.json carries NO boundary_url / boundary_deployment key (full file quoted, effectively, by this capture: db/host/kern/name/role/schema only) -- per the commission's own instruction this is recorded as evidence about this world's serving arrangement (it self-hosts its own ledger via direct psql/led, never through the boundary service), not a defect for this document to fix. No boundary-multiplex.toml exists at repo root either, and a live `ps aux` at capture time shows boundary_service processes only for OTHER worlds/fixtures (dest_a, experience's autoharn-panel, several setup-tui-config-file scratch worlds) -- none configured against schema=autoharn1
start_now = false    # moot given configure=false above, recorded for parity with the exemplar's own shape rather than left silently absent

# =============================================================================================
# [observability] -- WITNESSED
# =============================================================================================
[observability]
run = false    # no otelcol-config.yaml or otel-data/ directory exists anywhere in the tree for this world, and a live `ps aux` shows no otelcol/otel-watch process attached to schema=autoharn1 (the repo DOES contain `otel-attest`/`otel-watch` as repo-root VERBS/tooling under active development -- e.g. rows 1453/1457/1466/1713 -- but that is this project building the mechanism, not this world's own deployment running it as a configured daemon)
otelcol = false    # same evidence
otel_watch = false    # same evidence

# =============================================================================================
# [hydration] -- WITNESSED (ADR corpus, durable decisions) / GAP-NAMED (fork_provenance, role_charters)
# =============================================================================================
# ADR ADOPTION, per the world's OWN adoption record (not the law/adr/ directory listing alone,
# per the method rule): ledger row 511 (2026-07-13, kind=decision, "review-done: adr-
# portability-final-ratification") is the world's own blanket ratification of the entire ADR
# corpus refactor -- maintainer's three words: "(1) sec-7a Metz-rule demote-to-named-tradeoff
# ACCEPTED; (2) ADR-0001 RETIRE as chocofarm-only ...; (3) overall ratification YES". That row
# is this world's own adoption act for ADR-0000 and ADR-0002 through ADR-0018 (18 of 19
# numbers), with ADR-0001 the SAME row's own named exception -- confirmed on disk:
# law/adr/0001-immutability-and-copy-on-write.md is a tombstone ("Status: Retired-to-history",
# citing this same ratification), the live pre-refactor text moved verbatim to law/adr/history/
# 0001-immutability-and-copy-on-write.md. Row 1425 (2026-07-18) separately names ADR-0000,
# 0011, 0012, 0014, 0017, 0018 as MANDATORY full-text reading before touching kernel/law,
# corroborating (not the sole basis for) their live-binding status. Per-ADR ledger citation
# COUNTS (informational only, citation frequency is not itself the adoption test): every number
# 0000/0002-0005/0007-0014/0016-0018 is cited at least once in this ledger's 1888 rows;
# ADR-0006 and ADR-0015 have ZERO citations in this ledger's own text despite being covered by
# the same row-511 blanket ratification and present on disk -- named here so the zero-citation
# fact is visible, not silently treated as evidence of non-adoption (adoption is the row-511
# ratification act, not subsequent usage frequency; the method rule commits to the requirement
# -- was it ratified -- before the evidence -- was it cited).
[hydration]
run = true    # the ADR/durable-decision record above is manifestly engaged for this world
adopt_adrs = ["0000", "0002", "0003", "0004", "0005", "0006", "0007", "0008", "0009", "0010", "0011", "0012", "0013", "0014", "0015", "0016", "0017", "0018"]    # law/adr/*.md minus the one tombstone (0001), per row 511's blanket ratification + its own ADR-0001 exception, cross-checked against law/adr/history/0001-immutability-and-copy-on-write.md (the retired text) and law/adr/0001-immutability-and-copy-on-write.md (the live tombstone stub)

# DURABLE DECISIONS: all 14 catalog entries in tools/setup_tui/content/durable_decisions_data.py
# are themselves mined FROM this world's own ledger (each entry's own curation comment cites an
# autoharn1.ledger row id -- e.g. slug="makespan-scheduling-by-mandate"/"drift-backstops" cite
# row 1714; "tags-are-serious-business" row-mined per its own comment; "polychotomy-option-
# space-justification" cites row 1915) -- autoharn1 IS the source this catalog was curated
# from, not a downstream adopter selecting a subset, so all 14 are this world's own standing
# decisions by construction. ONE EXCEPTION NAMED: entry #10, slug
# "decomposition-to-unit-of-independent-resumption", is mined per its own comment from
# "autoharn-panel LIVE deployment CLAUDE.md point 1" -- a SIBLING deployment's record, not
# autoharn1.ledger directly; included below because it ships in this repo's own catalog file,
# but flagged since its evidentiary trail does not terminate in autoharn1's own ledger the way
# the other 13 do.
durable_decisions = ["makespan-scheduling-by-mandate", "drift-backstops", "single-branch-authoring", "tags-are-serious-business", "obligate-amplification-caution", "doc-currency-at-the-seam", "concurrent-builders-need-isolation", "runs-are-strictly-linear", "decomposition-to-unit-of-independent-resumption", "claims-carry-witnesses", "unanchored-review-briefs", "fresh-context-review-for-delegated-work", "polychotomy-option-space-justification"]    # 13 slugs -- durable_decisions_data.py's curation comments number 1-14, but #6 ("the setup surface is a maintained surface") was CUT per the genericity critique (row 1722: BESPOKE, addressed only to autoharn's own core contributors, no `slug`/CATALOG entry emitted for it, kept as an internal-only rule per row 1700 instead) -- 13 is the correct live count, verified by reading the module directly (not left as a hedge)

# GAP: fork_provenance -- no ledger row states whether autoharn1 itself, at its own founding,
# recorded a "forked from" provenance statement (the concept applies to a NEW world hydrating
# FROM a predecessor; autoharn1's own row 1 describes a MIGRATION of a work-tracking practice,
# not a fork-provenance declaration in the hydration screen's specific sense, and that screen
# postdates this world's birth regardless). Left unrecoverable rather than defaulted to false.
# fork_provenance = <GAP: unrecoverable, see comment above>
fork_provenance_statement = ""    # paired with the gap above, left blank rather than guessed

# GAP: role_charters -- searched for "charter registered"/"register-role-charter"/"role
# charter" text in autoharn1.ledger: zero hits for an actual CHARTER REGISTRATION event for any
# of the 7 principals (row 1668 merges the role_charter.py TOOL itself, which is a different
# fact than this world using it on its own roster). Also structurally non-round-trippable
# through --from-config even where it exists (config_seam.py's own synthesizer always declines
# it -- a charter needs a host-specific file path, excluded-by-type per spec §1) -- same
# permanent capture-gap class the known-good-blank exemplar names for its own principals-
# authority role-charter item, cited here as the FORMAT precedent, not as this world's evidence.
# role_charters = <GAP: unrecoverable / structurally non-round-trippable, see comment above>
