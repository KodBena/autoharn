seen-red/birth-standing-steps-scaffold — banked BOTH-POLARITY evidence (fixture-census)

Work item birth-standing-steps-scaffold (ledger row 270; design/
PHOENIX-SURVIVAL-UNIVERSE-2026-07-28.md §3/§7). Two operator-remembered birth steps that used to
require a by-hand fix after the fact (autoharn3 row 122's courier-registration fix; the autoharn3
cutover's by-hand `kernel.world_identity` INSERT) are now real birth acts inside
`bootstrap/new-project.sh`'s own --new-world sequence, with matching PASS/FAIL/SKIP checks added
to `bootstrap/templates/doctor.tmpl`.

Both polarities below are from a LIVE run of `seen-red/birth-standing-steps-scaffold/
run_fixtures.py` against the real toy Postgres db (192.168.122.1) — a real
`bootstrap/new-project.sh --new-world` birth (twice: once at the pre-fix merge-base commit,
once at the current tree), a real `./autoharn doctor` run against each, a real
`serving.boundary_service` instance, and a real `libexec/autoharn/courier` pull. Nothing stubbed
at the Python-object level. Both scratch worlds and their tempdirs were torn down cleanly after
this run (0 FAIL); left standing on any future failure.

RED (bootstrap/new-project.sh extracted live via `git show 74d8b2d:bootstrap/new-project.sh` —
74d8b2d is the merge-base commit this work item's fix landed on top of — into a sibling file
under this checkout's own `bootstrap/` so its relative AUTOHARN_ROOT/TEMPLATES resolution stays
correct; deleted immediately after use, never committed. The CURRENT `bootstrap/templates/
doctor.tmpl` is what runs against the resulting world -- only new-project.sh's own body differs
between RED and GREEN, confirming the two new doctor checks correctly FAIL against a genuinely
pre-fix-shaped world):

  RED birth: pre-fix new-project.sh --new-world exits 0 (the world births fine; it is simply
    missing the two acts this work item adds)
  RED birth transcript: no courier registration line, no world_identity seed line (confirms this
    is the true pre-fix shape, not an accidental no-op)

  RED doctor output (world bshredw):
    ### doctor -- is this world set up right?
        (/tmp/birth-standing-steps-fixture-ixrztl_u/bshredw)

    deployment.json                  PASS parsed (.../bshredw/deployment.json)
    database reachable               PASS 192.168.122.1/toy (SELECT 1 ok)
    schema + kern schema exist       PASS 'bshredw' and 'bshredw_kernel' both present
    lineage high-water/epoch         PASS chain_high_water.max_id=12, migration_epoch.epoch=0
    principals registered            PASS 4 row(s) in bshredw_kernel.principal
    courier principal registered     FAIL no 'courier' principal in bshredw_kernel.principal --
      every missive receipt this world pulls needs this identity to record arrivals as (design/
      FABLE-MISSIVES-KERNEL-SPEC.md §2.6). Fix: ./autoharn led register-principal courier tool
      --purpose "Records cross-world missive arrivals pulled over the boundary service -- and can
      write nothing else (kernel-scoped: validate_missive_courier_scope, s58; consult §4.3, Q3
      ratified row 1157). Never a deciding identity."
    world_identity populated         FAIL bshredw_kernel.world_identity has no row -- every
      missive write (send/receive/dispose) will refuse loudly until this is populated (design/
      FABLE-MISSIVES-KERNEL-SPEC.md §2.1). Fix (owner connection -- the granted role
      'bshredw_rw' holds no INSERT here by design, s58 §2.1's own grants): psql -h
      192.168.122.1 -d toy -v ON_ERROR_STOP=1 -c "INSERT INTO bshredw_kernel.world_identity
      (world_name) VALUES ('<this-world's-own-name>');"
    ./autoharn led answers a read query FAIL boundary_url absent from deployment.json (expected
      -- this run passes no --boundary-url; unrelated to this work item's own two checks)
    boundary URL                     SKIP not configured (expected, same reason)

    TOTAL: 3 FAIL, 5 PASS, 1 SKIP

  RED doctor: overall exit 1 (at least one FAIL) -- PASS
  RED doctor: 'courier principal registered' FAILs -- PASS
  RED doctor: courier FAIL teach-text names the fix command -- PASS
  RED doctor: 'world_identity populated' FAILs -- PASS
  RED doctor: world_identity FAIL teach-text names the owner-INSERT fix -- PASS

GREEN (the current, fixed bootstrap/new-project.sh, this checkout's working tree):

  GREEN birth: current new-project.sh --new-world exits 0 -- PASS
  GREEN birth transcript: courier registration line present
    "   (courier) registered through the boundary ceremony (class tool, registrar author, s58)"
  GREEN birth transcript: world_identity seed line present
    "   world_identity seeded: world_name = 'bshgrnw' (DB bshgrnw_kernel.world_identity)"

  GREEN doctor output (world bshgrnw):
    ### doctor -- is this world set up right?
        (/tmp/birth-standing-steps-fixture-ixrztl_u/bshgrnw)

    deployment.json                  PASS parsed (.../bshgrnw/deployment.json)
    database reachable               PASS 192.168.122.1/toy (SELECT 1 ok)
    schema + kern schema exist       PASS 'bshgrnw' and 'bshgrnw_kernel' both present
    lineage high-water/epoch         PASS chain_high_water.max_id=13, migration_epoch.epoch=0
    principals registered            PASS 5 row(s) in bshgrnw_kernel.principal
    courier principal registered     PASS registered (bshgrnw_kernel.principal)
    world_identity populated         PASS world_name='bshgrnw'
    ./autoharn led answers a read query FAIL boundary_url absent from deployment.json (expected,
      same unrelated reason as the RED leg -- this run passes no --boundary-url)
    boundary URL                     SKIP not configured (expected, same reason)

    TOTAL: 1 FAIL, 7 PASS, 1 SKIP

  GREEN doctor: 'courier principal registered' PASSes -- PASS
  GREEN doctor: 'world_identity populated' PASSes -- PASS

GREEN (a real courier pull against the birth-registered courier principal -- the row-122 class,
witnessed closed): the GREEN world (bshgrnw) is served by a real `serving.boundary_service`
instance; `libexec/autoharn/courier` is pointed at it plus a real (stdlib http.server, not
stubbed) mock counterpart ("bshcpartw") serving one well-formed outbound missive addressed to
bshgrnw:

    courier: bshcpartw: pulled=1 new=1 recorded=[14] dedup-raced=0

  courier pull setup: real boundary_service comes up for the birthed world -- PASS
  courier pull setup: mock counterpart HTTP server comes up -- PASS
  courier pull: exits 0 -- PASS
  courier pull: a missive_received row landed, authored by the birth-registered 'courier'
    principal (SELECT count(*) FROM ledger JOIN principal ON ... WHERE kind='missive_received'
    AND principal.name='courier' AND missive_author_world='bshcpartw' -> count=1) -- PASS

Full run: `birth-standing-steps-scaffold: all cases behaved as expected.` (exit 0). Both scratch
worlds (bshredw, bshgrnw) and their tempdir dropped after this run.
