seen-red/minimal-profile-tracker — banked RED evidence (both-polarity proof, fixture-census)

bootstrap/new-project.sh --profile tracker (ledger row 1271, retiring bootstrap/track-work.sh;
user-guide/TRACK-WORK-RETIREMENT-HERITAGE.md) is the modern standing work-tracking scaffold mode.
Both polarities below are from a LIVE run of
seen-red/minimal-profile-tracker/run_fixtures.py against the real toy@192.168.122.1 db, using a
throwaway schema (mptfixture/mptfixture_kernel/mptfixture_rw), torn down after.

CASE RED-USAGE (a required flag omitted — --name — refuses at argument-parsing time, before
any DB is touched):

  usage: bootstrap/new-project.sh <dest-dir> --db <db> --host <host> --schema <schema> --kern <kern> --role <role> [--name <name>] [--governed <patterns>] [--force]
         bootstrap/new-project.sh <dest-dir> --new-world <world> --db <db> --host <host> [--name <name>] [--governed <patterns>] [--force]
         bootstrap/new-project.sh <dest-dir> --profile tracker --name <name> --db <db> --host <host> [--schema <schema>]
             [--kern <kern>] [--role <role>] [--force]
  exit code: 2
  no schema was created (verified live: `SELECT to_regclass('mptfixture.ledger')` returns NULL)

CASE RED-BAD-NAME (--name containing characters outside the [a-z0-9]+ intersection this
profile requires — its own boundary_deployment/SQL-identifier allowlist conflict — refuses
before any DB is touched):

  new-project.sh: REFUSED -- --profile tracker's --name ('Has_Bad_Chars') must match
                  [a-z0-9]+ (lowercase letters and digits only) -- the intersection
                  of the boundary service's own deployment-name contract
                  ([a-z0-9-]{1,64}, no underscores/uppercase -- serving/
                  boundary_multiplex_config.py spec §2) and the SQL-identifier
                  allowlist ([A-Za-z0-9_]+, no hyphens) --name also derives
                  --schema/--kern/--role from. Pick a compliant --name, or supply
                  --boundary-url/--boundary-deployment explicitly with a compliant
                  label (and --schema/--kern/--role explicitly too, if --name itself
                  must stay outside [a-z0-9]+). Nothing was touched.
  exit code: 1
  no schema was created (verified live)

CASE RED-EXISTING (a second invocation against a directory that already carries a
deployment.json — refused before any DDL runs, ledger rows provably untouched):

  new-project.sh: /tmp/minimal-profile-tracker-fixture-XXXXXXXX/project/deployment.json already exists -- refusing to overwrite (pass --force to replace it).
  exit code: 1
  ledger row count before and after the refused re-run: 9 -> 9 (unchanged; verified live)

GREEN polarity (WITNESSED live, this build, 2026-07-25): GREEN-ADOPT (fresh scaffold — exit 0,
deployment.json + boundary-multiplex.toml + the ten SHIM_VERBS_ALL verbs + keys/ + attestations/
+ roles/ written, NO .claude/settings.json and NO root CLAUDE.md — "a standing project is not a
governed world", checked directly, not merely asserted); GREEN-BOUNDARY-AUTOSPAWN (no daemon
running after GREEN-ADOPT; the FIRST `./led work open` call spawns one automatically via
ensure-running and the write succeeds); GREEN-ROUNDTRIP (open -> list shows it open -> claim ->
close --review-deferred --witness -> list excludes the closed item); GREEN-DOCTOR (`./doctor`
reports 0 FAIL); GREEN-FORCE (a `--force` re-scaffold against the already-birthed deployment
succeeds, exit 0, ledger rows unchanged — this profile's own idempotent-force contract, mirrored
from the retired bootstrap/track-work.sh, is NOT the same as `--new-world`'s hard preflight
refusal). All cases run via seen-red/minimal-profile-tracker/run_fixtures.py's main(); exit 0,
scratch substrate torn down to zero residue (schema/kernel-schema/role dropped, spawned boundary
process killed, tempdir removed).
