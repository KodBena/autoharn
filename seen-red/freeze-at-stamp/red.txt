== source autoharn.ledger row count at fixture start: 229 ==

=== a-dest-exists-refused ===
  [ok] exit=1 stderr_tail='exists. Pass --force to overwrite, or pick\n  a fresh destination (a frozen snapshot is settled evidence; overwriting one in place\n  by default would silently discard whatever was frozen there before).'

=== b-bad-commit-refused ===
  [ok] exit=1 stderr_tail='rk/w/vdc/1/autoharn/.claude/worktrees/agent-a072fa383b7464382:\n  fatal: Needed a single revision\n  (git rev-parse --verify "not-a-real-ref-ever-xyz^{commit}" failed -- check the ref/sha and try again)' dest_created=False

=== c-as-of-id-beyond-max-refused ===
  [ok] exit=1 max_id=231 stderr_tail="freeze-at-stamp: REFUSED -- --as-of 1000230 names a ledger id beyond the source\n  tracker's own max id (231). Pick an id <= 231, or omit --as-of to\n  default to the commit's own committer timestamp."

=== d-as-of-future-ts-refused ===
  [ok] exit=1 stderr_tail="-at-stamp: REFUSED -- the cutoff timestamp '2099-01-01T00:00:00Z' is in the future\n  (server now() = 2026-07-12 20:13:44.535066+02). A frozen snapshot cannot include rows that have not\n  happened yet."

== live probe: db_exists=1 owner_role_exists=1 ro_role_exists=1 connect-as-owner reachable=True ==
   probe stderr: ''

=== e-standing-db-unreachable-refused ===
  [ok] SKIPPED (db is actually reachable now -- this polarity no longer applies; see case f below for the live end-to-end run instead)

=== f-full-snapshot-e2e ===
  [ok] script_exit=0 | row_id=1 present:True rows>200 present:False | verify-chain exit=0 stdout_tail='/lineage/s26-row-hash-chain.sql not applied here)\nverify-chain: tail-deletion witness check SKIPPED -- hash-chain status is UNAVAILABLE, already diagnosed above' | write-attempt exit=3 stderr_tail='SET\nSET\nERROR:  permission denied for table ledger'

=== g-source-row-count-invariant ===
  [ok] before=229 after=229 (source tracker must be untouched by every polarity above, refusal or success)

== zero residue: /tmp/freeze-at-stamp-fx-uyzzwbhx removed; no schema/db was created or dropped in toy by this fixture ==
ALL EXERCISED CASES OK (0 case(s) UNEXERCISED with a concrete, named blocker -- see above), zero residue, source row count invariant held.

-- Dated correction, 2026-07-27 (ADR-0005 Rule 8: the record above stands verbatim; this append
-- records the CURRENT state) -- retired-bare-shim straggler sweep (root-shim-pruning residue,
-- ledger row 1357): case f-full-snapshot-e2e's own code (`sh(["./verify-chain"], cwd=dest_f)`
-- and `sh(["./led", "decision", ...], cwd=dest_f)`) shelled to the retired bare per-verb shims
-- inside the frozen destination. Those calls were correct when the "ALL EXERCISED CASES OK"
-- record above was made (freeze-at-stamp.sh then wrote separate dest_f/verify-chain and
-- dest_f/led files via its own SHIM_VERBS_ALL rewrite loop) but that loop was collapsed by the
-- §6 amendment (rows 1357/1365/1366/1367, 2026-07-26): freeze-at-stamp.sh now writes ONE
-- dest_f/autoharn dispatcher, no per-verb files, so both calls -- and the docstring comment
-- describing them -- were stale. Fixed: repointed to `sh(["./autoharn", "verify-chain"], ...)`
-- and `sh(["./autoharn", "led", "decision", ...], ...)`; comment updated to match.
--
-- UNEXERCISED this date, concrete blocker (unrelated to the fix, unrelated to root-shim-
-- pruning): this worktree carries no deployment.json for this repo's own root tracker (a
-- worktree is deliberately left unwired), so `_dr.load_deployment(REPO / "deployment.json")`
-- at this fixture's own import time refuses before any case can run at all -- the SAME
-- boundary this build must respect (never wiring or writing to the real deployment from an
-- agent worktree). The fix above is verified by direct reading (bootstrap/freeze-at-stamp.sh's
-- own §6-amendment dispatcher-writer, confirmed to write only `$DEST/autoharn`) and by
-- mechanical equivalence (the dispatcher execs the identical bootstrap/templates/*.tmpl this
-- fixture's own PGUSER=$DB_RO_ROLE read-only role already expects), not by a live re-run of
-- case f itself. Re-witness this specific case next time this fixture runs from the main
-- checkout (or a worktree with a real, wired deployment.json) against a reachable
-- autoharn_test.
