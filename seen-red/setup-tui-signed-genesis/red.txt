seen-red/setup-tui-signed-genesis -- banked RED evidence (both-polarity proof, fixture-census)

design/FABLE-SETUP-TUI-SIGNED-GENESIS-SPEC.md §4's five witnesses (WG1-WG5), commission ledger
rows 1724/1725. This file banks real, observed output from a live run of
seen-red/setup-tui-signed-genesis/run_fixtures.py against a reachable Postgres cluster
(HARNESS_PGHOST=192.168.122.1, db toy) with a real `gpg` on PATH -- no mocks. Every scratch
world and every scratch GNUPGHOME was torn down before this evidence was captured (verified: no
`probeworld<base>*` schema/role left in `toy`, no `setup-tui-signed-genesis-scratch-*` directory
left under the system temp dir).

=== Live run, full summary (the fixture's own stdout) ===

WG1 ok: full ceremony (keygen -> export -> keys/ discharged -> commission 7 signed ->
verify-commission VERIFIED), zero scratch-GNUPGHOME residue
WG2 ok: tampered commission bytes -> verify-commission FORGED-OR-CORRUPT, exit 1 -- the gate
the ceremony's own step 4 invokes REFUSES on this input, never recording WITNESSED
WG4 ok: --dry-run against an already-ceremony'd world left the filesystem byte-identical,
showed placeholder <id>/<fingerprint> argv, recorded the verification row DRY-SKIPPED (never a
faked VERIFIED), zero scratch residue
WG3 ok: skip -> checklist SKIPPED, no .asc written, verify-commission UNSIGNED exit 0 on the
(still-unsigned) founding commission 7, world fully functional (legacy/led --recent still works)
WG5 ok: out-of-sequence entry refuses legibly, no traceback, on each of three independent
preconditions -- (a) nonexistent destination, (b) a real directory missing
keys/verify-commission/legacy-led, (c) gpg unreachable on PATH
ALL CASES OK -- setup_tui Signed genesis WG1-WG5, zero residue (scratch worlds, scratch
GNUPGHOMEs, the operator's real ~/.gnupg never touched)
exit=0

=== WG1: real command transcript excerpt (a manual dry-run of the same ceremony code path,
captured before this fixture existed, kept here as a fuller worked example than the fixture's
own terse assertions print) ===

$ /tmp/sg_smoke/sgprobe1784425463/legacy/led commission 'Founding commission: build the world
per the signed genesis ceremony fixture test.'
led: row 7 written.
$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-5mpe0ga_ --batch --generate-key
/tmp/setup-tui-signed-genesis-scratch-5mpe0ga_/keygen.batch
gpg: keybox '/tmp/setup-tui-signed-genesis-scratch-5mpe0ga_/pubring.kbx' created
gpg: /tmp/setup-tui-signed-genesis-scratch-5mpe0ga_/trustdb.gpg: trustdb created
gpg: directory '/tmp/setup-tui-signed-genesis-scratch-5mpe0ga_/openpgp-revocs.d' created
gpg: revocation certificate stored as
'/tmp/setup-tui-signed-genesis-scratch-5mpe0ga_/openpgp-revocs.d/AC0605DD42603F9BC65BF7D572C7900EBA157267.rev'
$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-5mpe0ga_ --armor --export
AC0605DD42603F9BC65BF7D572C7900EBA157267
-----BEGIN PGP PUBLIC KEY BLOCK-----
[... armored key text, wrote
/tmp/sg_smoke/sgprobe1784425463/keys/autoharn-setup-tui-fixture-key-throwaway.asc ...]
$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-5mpe0ga_ --batch --yes --pinentry-mode
loopback --passphrase setup-tui-fixture-passphrase-THROWAWAY-ONLY-never-a-real-key
--detach-sign --armor -o /tmp/sg_smoke/sgprobe1784425463/.claude/commission-7.asc -
$ /tmp/sg_smoke/sgprobe1784425463/verify-commission --id 7 --json
{
  "id": 7,
  "actor": "commissioner",
  "signing_mode": "FULL",
  "verdict": "VERIFIED",
  "detail": "statement sha256=f8bcb79909652158189bb7a55f4e37be46d6b43b651eef80b56ac54fbb0e92da.
gpg: Signature made Sun Jul 19 03:44:35 2026 CEST\ngpg:                using EDDSA key
AC0605DD42603F9BC65BF7D572C7900EBA157267\ngpg: Good signature from \"AUTOHARN SETUP-TUI FIXTURE
KEY -- THROWAWAY <setup-tui-fixture@example.invalid>\" [unknown]\ngpg: WARNING: This key is not
certified with a trusted signature!\ngpg:          There is no indication that the signature
belongs to the owner.\n      AC0605DD42603F9BC65BF7D572C7900EBA157267"
}

=== WG2: the red leg -- tampered .asc bytes, verify-commission (the exact verb the ceremony's
own gate step invokes) on the SAME commission id ===

$ /tmp/sg_smoke/sgprobe1784425463/verify-commission --id 7 --json
{
  "id": 7,
  "actor": "commissioner",
  "signing_mode": "FULL",
  "verdict": "FORGED-OR-CORRUPT",
  "detail": "statement sha256=f8bcb79909652158189bb7a55f4e37be46d6b43b651eef80b56ac54fbb0e92da.
gpg: CRC error; 66B3F7 - 51B1B1\ngpg: packet(2) with unknown version 10\ngpg: no signature
found\ngpg: the signature could not be verified.\nPlease remember that the signature file (.sig
or .asc)\nshould be the first file given on the command line."
}
exit=1

=== WG3: skip leaves the founding commission UNSIGNED, exit 0, world fully functional ===

$ LED_ACTOR=commissioner .../legacy/led commission "WG3 test: founding commission, never
signed."
led: row 7 written.
$ .../verify-commission --json
{
  "id": 7,
  "actor": "commissioner",
  "signing_mode": "FULL",
  "verdict": "UNSIGNED",
  "detail": "no .claude/commission-7.asc found — legitimate FULL-mode commission, not a
defect (spec §3: UNSIGNED is a weaker claim, never a failure)"
}
exit=0
$ .../legacy/led --recent 3
7|commission|WG3 test: founding commission, never signed.|commissioner||f|current
6|principal_registered|principal 'write-boundary' registered ...
5|principal_registered|principal 'commissioner' registered ...

=== WG4: --dry-run, zero filesystem delta (mechanical before/after tree hash), placeholder argv
never a fabricated id/fingerprint ===

$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-2k7c4kme --batch --generate-key
/tmp/setup-tui-signed-genesis-scratch-2k7c4kme/keygen.batch
  [dry-run: not executed]
$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-2k7c4kme --armor --export '<fingerprint>'
  [dry-run: not executed]
$ gpg --homedir /tmp/setup-tui-signed-genesis-scratch-2k7c4kme --batch --yes --pinentry-mode
loopback --passphrase setup-tui-fixture-passphrase-THROWAWAY-ONLY-never-a-real-key
--detach-sign --armor -o '/tmp/.../commission-<id>.asc' -
  [dry-run: not executed]
IDENTICAL (tree_before == tree_after, mechanical sha256-per-file comparison)

=== WG5: out-of-sequence refusals, no traceback ===

  REFUSED: destination directory '/tmp/.../wg5_missing' does not exist -- nothing to run the
ceremony against. Run a birth first (or check the path), then retry this screen.
  REFUSED: /tmp/.../wg5_bare is missing keys/, verify-commission, legacy/led -- this does not
look like a world this project's own scaffold produced. Nothing done.
  REFUSED: 'gpg' is not on PATH -- the Signed genesis ceremony needs GnuPG installed
(user-guide/USER-GPG-TRUST-LAYER-FAQ.md). Nothing done.

Zero residue verified after every leg above: `psql -h 192.168.122.1 -d toy -tAc "select nspname
from pg_namespace where nspname like 'probeworld<base>*'"` empty; no
`setup-tui-signed-genesis-scratch-*` directory left under the system temp dir; the operator's
own `~/.gnupg` untouched throughout (every keygen in this fixture used
`tools.setup_tui.signed_genesis.keygen_scripted`'s own scratch GNUPGHOME, never the ambient
default).
