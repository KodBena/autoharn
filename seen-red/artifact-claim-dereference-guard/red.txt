Banked RED evidence for night-build-defect-repair DEFECT 2 (bootstrap/templates/led.tmpl's
--evidence path-dereference guard). Captured by running
seen-red/artifact-claim-dereference-guard/run_fixtures.py against the PRE-FIX led.tmpl (b77ee7a's
own shipped version, before this session's night-build-defect-repair fix): the bare-directory
REFUSE cases and the statement-path WARN cases all FAIL, reproducing exactly the two verifier
findings (directory acceptance via bare `test -e`; $statement never scanned at all).

# RED — dead --evidence path: MUST REFUSE, no row written
  [ok] guard REFUSES (nonzero exit)
  [ok] teach-text cites the specimen class (896-899)
  [ok] no row written (max id unchanged)
# RED — bare directory --evidence (no trailing slash): MUST REFUSE, no row written
  [FAIL] guard REFUSES the bare directory (nonzero exit)
  [FAIL] teach-text names it a DIRECTORY, not a file
  [FAIL] teach-text points at the trailing-slash opt-in
  [FAIL] no row written (max id unchanged)
# GREEN — a live --evidence FILE: MUST ACCEPT, real row lands, round-trip verified
  [ok] guard ACCEPTS (exit 0)
  [ok] real led round-trip: led show <id> reads the row back
# GREEN — --evidence directory cited via trailing slash: MUST ACCEPT
  [ok] guard ACCEPTS the explicitly-cited directory (exit 0)
  [ok] round-trip verified
# GREEN — dead path-shaped token in STATEMENT prose: WARN-ONLY, still writes
  [ok] write still succeeds (exit 0, warn-only, not a refusal)
  [FAIL] a WARNING is printed naming the path-shaped token
  [FAIL] warning states the warn-only/refuse asymmetry
  [ok] round-trip verified
# GREEN — row:<id> citation in statement: untouched, no warning, writes clean
  [ok] write succeeds (exit 0)
  [ok] no path-shape WARNING fires for a row: citation
# GREEN — URL in statement: untouched, no warning, writes clean
  [ok] write succeeds (exit 0)
  [ok] no path-shape WARNING fires for a URL

SPECIMEN INERT — 6 check(s) failed: ['guard REFUSES the bare directory (nonzero exit)',
'teach-text names it a DIRECTORY, not a file', 'teach-text points at the trailing-slash opt-in',
'no row written (max id unchanged)', 'a WARNING is printed naming the path-shaped token',
'warning states the warn-only/refuse asymmetry']

After the fix, the same run_fixtures.py goes fully green (all 7 cases, real led round-trips
verified) -- see this session's commit message / ledger rows for the green capture and the
written row ids.

---

Second banked RED capture, AUTOHARN_BACKFLOW finding 5 (bootstrap/templates/legacy-led.tmpl's
warn_path_shaped_in_statement(): the 6-line WARN-ONLY explanation block sat INSIDE the
`for word in $text` loop, so a statement flagging N path-shaped tokens printed the entire
paragraph N times instead of once). Captured by running run_fixtures.py's new
green_statement_multiple_path_tokens_single_preamble() case against the PRE-FIX function (`git
stash` of the fix commit's legacy-led.tmpl hunk, fixture re-run, then unstashed): a statement
with three flagged tokens fires the "led: WARNING -- the statement contains" preamble line three
times and the "WARN-ONLY" sentence three times, reproducing the finding exactly.

# GREEN — THREE dead path-shaped tokens in one STATEMENT: ONE preamble, one list, still writes (AUTOHARN_BACKFLOW finding 5)
  [ok] write still succeeds (exit 0, warn-only, not a refusal)
  [FAIL] the explanation preamble prints EXACTLY ONCE, not once per token
  [ok] all three flagged tokens are listed
  [FAIL] warning states the warn-only/refuse asymmetry (once)
  [ok] round-trip verified

SPECIMEN INERT — 2 check(s) failed: ['the explanation preamble prints EXACTLY ONCE, not once per
token', 'warning states the warn-only/refuse asymmetry (once)']

After the fix (boilerplate hoisted out of the loop: tokens accumulated into a `flagged` array,
the six-line preamble + a trailing per-token list printed once after the loop), the same case
goes green: one preamble, "WARN-ONLY" once, all three tokens listed -- see this commission's
commit for the green capture.

---

2026-07-23 dated note (fixture-repairs review, batch a74978e0): TWO separate follow-ups landed
against this fixture, both witnessed live against scratch (HARNESS_PGHOST=192.168.122.1):

1. SCRATCH REWIRE (SEVERE): this fixture used to drive the repo-root `./led` (ROOT/parents[2])
   against THIS CHECKOUT'S OWN deployment.json -- whatever real deployment the checkout happened
   to be pointed at. That register produced a live incident: during this same batch, the
   fixture's own probe writes landed 8 garbage rows in the REAL kernel (ledger rows 1237-1244,
   marked garbage by finding row 1248) because deployment.json resolved to a live deployment,
   not a scratch one. The fixture now scaffolds its own scratch world via `bootstrap/
   new-project.sh --new-world` and stands a real `serving.boundary_service` against it via
   `serve_existing_world` (seen-red/boundary-service/run_fixtures.py), exactly the pattern its
   sibling fixtures already use. It never again touches this checkout's own deployment.json.

2. SPECIMEN INERT, NOW REDISCOVERED-GAP (the guard this fixture proves does not exist in the
   served CLI at all -- ledger row 1245, open work item): live-witnessed against the CURRENT
   served `led`, BOTH the --evidence path-dereference guard AND the path-shaped-statement
   warning are entirely absent from the rebased CLI (a rebase feature-loss, not a fixture bug).
   Every case that used to expect a REFUSAL (dead evidence path, bare directory) is now silently
   ACCEPTED; every case that used to expect a WARNING (statement-path single/multi token) now
   writes with NO warning at all. The fixture's cases were rewritten to assert this OBSERVED
   CURRENT BEHAVIOR as NAMED REDISCOVERED-GAP-* cases (the same treatment seen-red/
   led-help-token-closure/run_fixtures.py already banks for its own rediscovered gap), rather
   than silently passing or silently failing against a guard that no longer exists. The
   specimen is INERT pending row 1245's repair: once the guard/scanner is reimplemented in the
   served path, the REDISCOVERED-GAP-* cases' assertions must be flipped back to the original
   RED (refuse)/WARN (warn-but-write) expectations -- at which point they stop being gaps and
   this note's INERT framing is no longer accurate.

Witnessed live, scratch only, all 8 cases green against the current (gap-carrying) CLI shape --
see this commit's own message for the run's exact output.

---

2026-07-25 dated note (row 1245's own repair, this session): RED-FIRST capture against the
served CLI BEFORE `_evidence_dereference_violation`/`_warn_path_shaped_in_statement` were added
to bootstrap/templates/led.tmpl -- this is the fixture AS IT STOOD after the 2026-07-23
REDISCOVERED-GAP rewrite above, run against the pre-repair led.tmpl (scratch, HARNESS_PGHOST=
192.168.122.1):

# REDISCOVERED-GAP-DEAD-EVIDENCE-PATH -- a dead --evidence path used to REFUSE; row 1245: the guard is gone, so it is now silently ACCEPTED. Named, not silenced.
  [FAIL] OBSERVED: guard does NOT refuse (exit 0)
  [FAIL] OBSERVED: a row WAS written (max id advanced by one) -- the gap, reproduced
  [FAIL] OBSERVED: no teach-text about the 896-899 specimen class (nothing to cite -- the guard was never consulted)
# REDISCOVERED-GAP-BARE-DIRECTORY-EVIDENCE -- a bare (no trailing slash) EXISTING directory used to REFUSE; row 1245: now silently ACCEPTED. Named, not silenced.
  [ok] OBSERVED: guard does NOT refuse the bare directory (exit 0)
  [ok] OBSERVED: a row WAS written (max id advanced by one) -- the gap, reproduced
# REDISCOVERED-GAP-STATEMENT-PATH-NO-WARNING -- a dead path-shaped token in STATEMENT prose used to WARN-but-write; row 1245: it still writes, but the warning no longer fires at all (the scanner is gone). Named, not silenced.
  [ok] write still succeeds (exit 0, unaffected)
  [FAIL] OBSERVED: NO warning is printed at all (the scanner does not exist -- the gap, reproduced; this is NOT the guard correctly staying silent, it is the guard being absent)
# REDISCOVERED-GAP-STATEMENT-MULTI-PATH-NO-WARNING -- THREE dead path-shaped tokens in one STATEMENT used to fire ONE preamble + a list (AUTOHARN_BACKFLOW finding 5's own fix); row 1245: now NO warning fires at all, for any count. Named, not silenced.
  [ok] write still succeeds (exit 0, unaffected)
  [FAIL] OBSERVED: NO preamble line fires (finding 5's own fix has nothing left to regress -- there is no warning mechanism left to over-print)
  [FAIL] OBSERVED: none of the three tokens are echoed in a warning list (nothing scans for them)

SPECIMEN INERT -- 6 check(s) failed against the OBSERVED CURRENT BEHAVIOR the fixture asserted
at that point (the gap, faithfully reproduced): the guard/scanner were absent exactly as row
1245 named.

A SECOND, SEPARATE fixture bug was found and fixed in the SAME repair pass while banking this
capture: the 2026-07-23 SCRATCH REWIRE had silently truncated the bare-directory probe's own
value from the original fixture's "bootstrap/templates" (a real path with a "/", git show
6361c82) down to a bare "bootstrap" -- which is not PATH-SHAPED at all by this guard's own rule
(no leading `/`/`./`, no `/` anywhere), so that case's assertions were VACUOUS even before this
row's repair (never actually exercising the directory-vs-file check either polarity). Fixed by
switching to ".claude/logs", a real existing directory every `--new-world` scaffold carries that
genuinely contains a "/". Re-run against the SAME pre-repair led.tmpl after that fixture fix:

# REDISCOVERED-GAP-BARE-DIRECTORY-EVIDENCE -- (corrected probe value)
  [ok] OBSERVED: guard does NOT refuse the bare directory (exit 0)
  [ok] OBSERVED: a row WAS written (max id advanced by one) -- the gap, reproduced

After the repair (guard/scanner reimplemented in cmd_generic/cmd_review), the SAME fixture (with
its REDISCOVERED-GAP-* case bodies flipped to assert the guard holds, this session) goes fully
green, all 8 cases, real led round-trips verified, scratch torn down to zero residue -- see this
session's own commit for the green capture.

---

2026-07-25 dated note (strengthened-tier fresh-context review of THIS arc's own guard-trio
commit, CLEARED-BUT-MODERATE-SILENT, MODERATE finding fixed same session): the review found a
third sibling guard silently missing. legacy-led.tmpl's `review` case calls
`warn_content_free_review_statement "$statement"` immediately alongside
`warn_path_shaped_in_statement "$statement"` (git show 93affa0^, both calls back to back right
after `statement="$*"`), but the row-1245 repair above only ported the path-shape scanner into
`cmd_review` -- the content-free sibling was never wired in. Downstream mitigation was partial
only: `engine/review_gap_audit.py` flags a short review, but ONLY when it discharges a
countersign obligation; a content-free `led review` statement outside that path got no catch
anywhere in the served CLI.

RED-FIRST capture, TWO new cases added to this fixture (red_first_content_free_review_statement /
green_genuine_review_statement_no_warning), run against led.tmpl with
`_warn_content_free_review_statement(statement)`'s call site in `cmd_review` commented out
(the state of this arc's own prior commit, 5e6fd5c, before this same-session follow-up fix;
scratch, HARNESS_PGHOST=192.168.122.1):

# RED-FIRST-CONTENT-FREE-REVIEW-STATEMENT -- `led review` on a content-free statement (run12 ledger row 20's own 4-char specimen, verbatim) still writes (WARN-only, never a refusal), and now prints a WARNING naming the content-free-review-audit tracker item (strengthened-tier review finding, this arc: the guard was byte-faithfully ported off legacy-led.tmpl's own warn_content_free_review_statement, but silently left un-wired from cmd_review when the sibling path-shape scanner was reinstated).
  [ok] write still succeeds (exit 0, WARN-only, never a refusal)
  [FAIL] a WARNING is printed (the content-free guard fires)
  [FAIL] the warning names this tracker item
  [FAIL] the warning points at the retroactive check (./audit --review-gap)
  [FAIL] the warning states the normalized 4-char length of the specimen
# GREEN, RIGHT REASON VERIFIED — a review statement of ordinary length gets NO content-free warning: verified for the right reason (the guard exists and correctly passes a genuine statement), not merely because nothing checks at all.
  [ok] write succeeds (exit 0)
  [ok] no content-free WARNING fires for a genuine-length statement

SPECIMEN RED — 4 check(s) failed against the EXPECTED (guard-holds) BEHAVIOR this fixture now
requires: ['a WARNING is printed (the content-free guard fires)', 'the warning names this
tracker item', 'the warning points at the retroactive check (./audit --review-gap)', 'the
warning states the normalized 4-char length of the specimen'] -- reproducing the finding exactly
(the write proceeds silently, no catch anywhere in the served CLI).

After the fix (`_warn_content_free_review_statement` ported into `bootstrap/templates/led.tmpl`
as a direct top-of-file import of `engine/review_gap_thresholds` -- house Python idiom, this file
is already Python so no subprocess round-trip is needed the way bash needed one -- and wired into
`cmd_review` alongside `_warn_path_shaped_in_statement`, legacy's own call order preserved), the
SAME fixture goes fully green, all 10 cases, real led round-trips verified, scratch torn down to
zero residue -- see this session's own commit for the green capture.
