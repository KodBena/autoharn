seen-red/setup-tui-principals-authority -- banked RED evidence (both-polarity proof, fixture-census)

design/FABLE-SETUP-TUI-PRINCIPALS-AUTHORITY-SPEC.md §4's six witnesses (WP1-WP6), commission
ledger row 1727. This file banks real, observed output from a live run of
seen-red/setup-tui-principals-authority/run_fixtures.py against a reachable Postgres cluster
(HARNESS_PGHOST=192.168.122.1, db toy) -- no mocks. Every scratch world (the WP1/WP2/WP3/WP5/WP6
scaffold and WP4's hand-built s40-only schema) was torn down before this evidence was captured
(verified: no `probeworld<base>*`/`s40only<base>*` schema/role left in `toy`, no
`setup-tui-principals-authority-*` directory left under the system temp dir).

=== Live run, full summary (the fixture's own stdout) ===

WP1 ok: register=7 competence=8 relation=9 charter=10, all four verified via `led show`
WP2 ok: duplicate registration of 'reviewer' refused, kernel teach-text rendered verbatim, no
traceback
WP3 ok: unregistered-role charter -- decline leaves a legible REFUSED with the manual command;
accept completes charter with both row ids echoed (register=11, charter=12)
WP5 ok: lesson + exact argv shown, '[dry-run: not executed]', checklist WOULD-DO, zero rows
actually written (verified by direct psql count)
WP6 ok: out-of-sequence entry refuses legibly against (a) a nonexistent destination and (b) a
real directory with no legacy/led, no traceback
WP4 ok: against a hand-built s40-only world, the bindings section reports unavailable-with-reason
and offers neither a competence-grant nor a typed-relation prompt
ALL CASES OK (WP1-WP6) -- principals & authority screen, zero residue

=== The RED leg (both-polarity) -- WP2/WP3-decline/WP4/WP6 ARE the red leg ===

This screen's own "red" (the discriminating case a broken build would fail) is not a single
tamper-and-detect step the way a signature-forgery witness is -- it is the REFUSAL surface
itself: WP2 (duplicate registration must be refused, never silently accepted or silently
succeeding a second time), WP3's decline leg (an unregistered charter role must refuse legibly,
never dead-end past a trap or silently proceed unregistered), WP4 (the s41 section must refuse to
OFFER anything on an s41-absent world, never crash and never silently pretend the vocabulary is
there), and WP6 (out-of-sequence entry against a missing/malformed destination must refuse, never
traceback). All four are exercised live above and all four refused exactly as this spec's own
witnesses require -- the GREEN leg is WP1/WP5 (the constitutive pass and the dry-run rehearsal
actually succeed and actually write/compute what they claim).

Re-run: HARNESS_PGHOST=192.168.122.1 python3 seen-red/setup-tui-principals-authority/run_fixtures.py
