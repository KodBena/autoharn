RED evidence for seen-red/workflow-drive-dead-legacy-led-default (ledger rows 1307/1308,
2026-07-26). Captured live against a real scaffolded scratch world (bootstrap/new-project.sh
--new-world wfdll1, real serving.boundary_service subprocess, real `./legacy/led` teaching stub)
BEFORE this fix landed -- git HEAD at commit 153a078, tools/workflow_compile.py's DRIVE_TEMPLATE
still carrying `led = "./legacy/led"` for check_charter/fetch_brief. Reproduced by importing a
compiled tools/workflow_units/*/drive.py module and calling its own check_charter() function --
the exact call this pass fixed -- against ./legacy/led, from inside the scaffolded world
directory (the same run_fixtures.py this directory banks re-proves this live, as a standing
regression, since the RED side needs the pre-fix hardcoded default which this worktree no longer
carries).

=== the defect: check_charter() against the pre-fix hardcoded default ===
$ python3 -c "
import importlib.util
spec = importlib.util.spec_from_file_location(
    'drive', 'tools/workflow_units/autoharn-builder-wave/drive.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
rc, out = mod.check_charter('./legacy/led', 'author')
print('rc:', rc)
print(out)
"
rc: 1
role_charter: REFUSED -- './legacy/led current 100000' failed:
legacy/led: RETIRED 2026-07 (design/FABLE-LEGACY-LED-RETIREMENT-SPEC.md) -- every surface
  serves through ./led now; the boundary is mandatory at every birth, and led principal *
  (grant-competence/relate and their 11 siblings) closed the one family the served path
  was ever missing. Use ./led instead.

-- The consequence this closes: check_charter's rc is 1 UNCONDITIONALLY (legacy/led is a pure
exit-1 stub, never a working CLI, since commit 93affa0 2026-07-23) -- regardless of whether an
actual, in-force charter registration exists for the principal. Every phase of every compiled
workflow resolves UNCHARTED by default, and dispatch falls to BLOCKED-uncharted (or the raw-
prose fallback under --allow-uncharted) -- the row-1663 charter+brief commission is dead in
production dispatch unless an operator manually passes --led ./led to every compiled drive.py
invocation. Confirmed independently: even with a REAL charter registered for 'author' (row 7,
`role-charter registered: role=author path=roles/author-CHARTER.md sha256=...`, IN-FORCE per
`tools/role_charter.py show author --led ./led` returning exit 0), the SAME check_charter() call
through the hardcoded `./legacy/led` default still returns rc=1 with the identical stub text
above -- the charter's existence is irrelevant to the pre-fix outcome, which is the SEVERE part
of this finding.

Fix (commit cb5cf23): tools/workflow_compile.py's DRIVE_TEMPLATE now defaults `led = "./led"`
(the served shim), the false "role_brief.py's parsers still silently misparse served output"
rationale is deleted (it was already false as of commit 417b200 on the in-flight role branch --
parse_current_line refuses loudly now, never silently misparses), and all 7
tools/workflow_units/*/drive.py copies were regenerated through tools/workflow_compile.py's own
compiler entry point (compile_toml/main).

HONEST PROVENANCE (correcting this file's own prior claim, review finding 1, 2026-07-26
follow-up): the sentence above said those 7 copies were "never hand-edited" -- that was false
for two lines in each. At cb5cf23 time, tools/workflow_compile.py had no notion of a repo-root
override; its `REPO_ROOT = Path(__file__).resolve().parents[1]` baked whatever checkout the
compiler ran from into `ROLE_CHARTER_PY`/`ROLE_BRIEF_PY`. The regeneration ran from this repo's
WORKTREE checkout, so those two lines in all 7 files came out baked to the worktree's own
transient `.claude/worktrees/<agent>/...` path -- wrong for a committed artifact meant to run
against the real checkout. Those two lines, in all 7 files, were then corrected BY HAND to the
real checkout's path; the commit message half-disclosed this, this file did not.

THIS FOLLOW-UP PASS (review finding 2) closes the gap instead of leaving it a one-time manual
patch: tools/workflow_compile.py gained an honest `--repo-root` flag / `AUTOHARN_REPO_ROOT` env
var (refusing a nonexistent path) so a compile invoked from a worktree can bake the REAL
checkout's path without hand-editing afterward, run_fixtures.py's new W5 case extends the
standing regression to catch a future worktree-path recurrence in the committed copies, and all
7 copies were regenerated AGAIN through the entry point WITH the override -- this time
genuinely never hand-edited, byte-identical to a fresh compile. run_fixtures.py in this
directory re-proves the fixed, GREEN side as a standing regression: the identical
check_charter() call against the served ./led, after registering the same charter via
tools/role_charter.py's own register flow, now returns rc=0 with "IN-FORCE charter
registration" in its output -- and (W1b, added this pass) the SAME call against the pre-fix
`./legacy/led` default still returns rc=1 with the identical stub text even AFTER that real,
in-force charter is registered, re-proving the severe part of this finding as a standing case
rather than leaving it only in this banked transcript.

================================================================================================
ROUND 2 RED (2026-07-26, fix round, strengthened-review BLOCKING finding 1). Captured live,
HARNESS_PGHOST=192.168.122.1, against this fixture's own run_fixtures.py as it stood at branch
tip b3b116d -- i.e. BEFORE this round's DRIVE_TEMPLATE/HYDRATE_TEMPLATE edit, with the ROUND-1
fix above (`led = "./led"`) still in place, run against the scaffold-umbrella migration
(rows 1357/1365/1366/1367) that landed in the SAME branch's own history and retired the bare
`./led` shim project-wide. Banked BEFORE touching tools/workflow_compile.py this round, per
CLAUDE.md's red-first discipline.

$ HARNESS_PGHOST=192.168.122.1 python3 seen-red/workflow-drive-dead-legacy-led-default/run_fixtures.py
[... W1/W1b PASS unchanged, elided ...]
== W2: GREEN -- check_charter() against ./autoharn led (the post-fix served default) ==
=== w2-green-exit-0 ===
  [FAIL] rc=1

=== w2-green-in-force ===
  [FAIL] out="role_charter: REFUSED -- './autoharn led current 100000' failed:\ncould not execute
  './autoharn led': [Errno 2] No such file or directory: './autoharn led'"

=== w2-green-no-drift ===
  [ok] out="role_charter: REFUSED -- './autoharn led current 100000' failed:\ncould not execute
  './autoharn led': [Errno 2] No such file or directory: './autoharn led'"

FAILURES: ['w4-autoharn-builder-wave-committed-matches-fresh', 'w2-green-exit-0',
'w2-green-in-force']

READING THIS RED: the w4 failure is because the COMMITTED autoharn-builder-wave/drive.py had
ALREADY been hand-patched (out of sync with the other six units and with DRIVE_TEMPLATE itself)
to default to "libexec/autoharn/led" under a DIFFERENT justification comment ("root-shim-pruning,
ledger row 1357") -- discovered while diffing this round's regeneration output, itself an
instance of the SAME "committed generated file silently hand-edited, drifted from its template"
hazard class W5 above already exists to catch (there, for the two ROLE_*_PY lines). The w2
failures are "could not execute './autoharn led'" -- Python's subprocess treating the two-token
override as ONE nonexistent literal filename, because `role_charter.py`'s own `run_led` at the
BAKED, real-checkout (`main`) absolute path predates that branch's shlex-split fix -- see this
round's fix-round commit message and the module docstring's "SECOND FIX ROUND" section for the
full disposition (W2 -> UNEXERCISED with a disclosed blocker; W6/W7 added, proving the actual
in-scope fix -- drive.py's own `run_led` default + shlex-split -- directly, without crossing that
checkout boundary).

Fix this round: DRIVE_TEMPLATE's/HYDRATE_TEMPLATE's `led` default flips from `"./led"` (itself
now dead) to the single-token `"libexec/autoharn/led"`, `run_led` gains `shlex.split(led)` before
exec, and all 7 `tools/workflow_units/*/{drive.py,hydrate.sh}` pairs are regenerated (never
hand-edited) through the compiler's own `--repo-root` machinery, restoring the previously
hand-patched autoharn-builder-wave copy to a genuinely generated, template-consistent state
along with the other six.
