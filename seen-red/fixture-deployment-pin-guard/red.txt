GREEN ok: GREEN-real-tree
GREEN ok: RED_REPO_LED
  /tmp/fixture-deployment-pin-guard-jbihm1si/red-repo-led.py:8: subprocess call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path) instead of a scaffolded scratch copy — this repo's own root-level verb shims resolve their deployment from their own file location, never from a caller's env override (fixture-scratch-pinning-guard, ledger row 1249; the row-1237-1244/1248 leak class)
GREEN ok: RED_REPO_LEGACY_PICKUP
  /tmp/fixture-deployment-pin-guard-jbihm1si/red-repo-legacy-pickup.py:8: subprocess call invokes THIS CHECKOUT'S OWN `pickup` (a REPO-rooted path) instead of a scaffolded scratch copy — this repo's own root-level verb shims resolve their deployment from their own file location, never from a caller's env override (fixture-scratch-pinning-guard, ledger row 1249; the row-1237-1244/1248 leak class)
GREEN ok: RED_ENVIRON_MUTATION
  /tmp/fixture-deployment-pin-guard-jbihm1si/red-environ-mutation.py:4: `os.environ["PICKUP_DEPLOYMENT"] = ...` mutates the PROCESS-GLOBAL environment instead of pinning per-subprocess (build a local `env = {**os.environ, "PICKUP_DEPLOYMENT": ...}` dict and pass it via each subprocess call's own `env=` kwarg instead — fixture-scratch-pinning-guard, ledger row 1249: "env pinned per-subprocess, never inherited")
GREEN ok: GREEN_SCRATCH_SCOPED
GREEN ok: GREEN_LOCAL_ENV_DICT

ALL CASES OK -- fixture-deployment-pin-guard both polarities, zero residue

=== 2026-07-26 fix-round 2 (fresh-context strengthened-tier review BLOCKED 26c7c48) =============
Full run of seen-red/fixture-deployment-pin-guard/run_fixtures.py after closing findings 1-5/7
(finding 6 disclosed, not fixed -- see gates/fixture_deployment_pin_guard.py's own POSTURE
section). Bank verbatim below; the seven new RED specimens (RED_WAIVER_BLANKET through
RED_SEMICOLON_SHARED_WAIVER) are this round's own red-first evidence.

GREEN ok: GREEN-real-tree
GREEN ok: RED_REPO_LED
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-repo-led.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_REPO_LEGACY_PICKUP
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-repo-legacy-pickup.py:8: call invokes THIS CHECKOUT'S OWN `pickup` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_MUTATION
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-environ-mutation.py:4: `os.environ["PICKUP_DEPLOYMENT"] = ...` (directly or aliased) mutates the PROCESS-GLOBAL environment instead of pinning per-subprocess (build a local `env = {**os.environ, "PICKUP_DEPLOYMENT": ...}` dict, pass via each call's own `env=` kwarg -- row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: GREEN_SCRATCH_SCOPED
GREEN ok: GREEN_LOCAL_ENV_DICT
GREEN ok: RED_OS_SYSTEM
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-os-system.py:8: `os.system(...)` spawns via a shell string, never an argv list -- refused outright (unenumerable repo-path spelling; use subprocess.run([...])). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SHELL_TRUE
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-shell-true.py:8: subprocess call with shell=True spawns via a shell string -- refused outright, same reason as os.system(...) above. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_LIBEXEC_LED
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-libexec-led.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_UPDATE
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-environ-update.py:4: `os.environ.update(...)` (directly or aliased) merges PICKUP_DEPLOYMENT into the PROCESS-GLOBAL environment -- same violation as a direct subscript assign (row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_ALIAS
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-environ-alias.py:5: `os.environ["PICKUP_DEPLOYMENT"] = ...` (directly or aliased) mutates the PROCESS-GLOBAL environment instead of pinning per-subprocess (build a local `env = {**os.environ, "PICKUP_DEPLOYMENT": ...}` dict, pass via each call's own `env=` kwarg -- row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_WRAPPER_INDIRECTED
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-wrapper-indirected.py:14: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: GREEN_WAIVED_LED
GREEN ok: RED_WAIVER_BLANKET
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-waiver-blanket.py:15: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SUBSCRIPT_MUTATION
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-subscript-mutation.py:11: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_APPEND_BUILT_ARGV
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-append-built-argv.py:12: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_OS_PATH_JOIN
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-os-path-join.py:9: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_JOINPATH
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-joinpath.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_PARTIAL_ARGV
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-partial-argv.py:10: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SEMICOLON_SHARED_WAIVER
  /tmp/fixture-deployment-pin-guard-hp30ieei/red-semicolon-shared-waiver.py:9: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.

ALL CASES OK -- fixture-deployment-pin-guard both polarities, zero residue

=== 2026-07-26 fix-round 4 (fresh-context strengthened-tier review of round 3; CENSUS FAIL-CLOSED) ===
Full run of seen-red/fixture-deployment-pin-guard/run_fixtures.py after closing findings 1-6
(rule A census sweep + findings 4/5/6's targeted detectors/verb-detection extensions; rule B/C in
their brief-requested sensitivity-independent form NOT implemented -- measured and declined, see
gates/fixture_deployment_pin_guard.py's own MEASUREMENT section). Bank verbatim below; the
thirteen new specimens (RED_CHAINED_ASSIGN through GREEN_MUTATED_NON_SENSITIVE) are this round's
own red-first evidence.

GREEN ok: GREEN-real-tree
GREEN ok: RED_REPO_LED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-repo-led.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_REPO_LEGACY_PICKUP
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-repo-legacy-pickup.py:8: call invokes THIS CHECKOUT'S OWN `pickup` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_MUTATION
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-environ-mutation.py:4: `os.environ["PICKUP_DEPLOYMENT"] = ...` (directly or aliased) mutates the PROCESS-GLOBAL environment instead of pinning per-subprocess (build a local `env = {**os.environ, "PICKUP_DEPLOYMENT": ...}` dict, pass via each call's own `env=` kwarg -- row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: GREEN_SCRATCH_SCOPED
GREEN ok: GREEN_LOCAL_ENV_DICT
GREEN ok: RED_OS_SYSTEM
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-os-system.py:8: `os.system(...)` spawns via a shell string, never an argv list -- refused outright (unenumerable repo-path spelling; use subprocess.run([...])). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SHELL_TRUE
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-shell-true.py:8: subprocess call with shell=True spawns via a shell string -- refused outright, same reason as os.system(...) above. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_LIBEXEC_LED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-libexec-led.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_UPDATE
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-environ-update.py:4: `os.environ.update(...)` (directly or aliased) merges PICKUP_DEPLOYMENT into the PROCESS-GLOBAL environment -- same violation as a direct subscript assign (row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ENVIRON_ALIAS
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-environ-alias.py:5: `os.environ["PICKUP_DEPLOYMENT"] = ...` (directly or aliased) mutates the PROCESS-GLOBAL environment instead of pinning per-subprocess (build a local `env = {**os.environ, "PICKUP_DEPLOYMENT": ...}` dict, pass via each call's own `env=` kwarg -- row 1249). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_WRAPPER_INDIRECTED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-wrapper-indirected.py:14: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: GREEN_WAIVED_LED
GREEN ok: RED_WAIVER_BLANKET
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-waiver-blanket.py:15: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SUBSCRIPT_MUTATION
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-subscript-mutation.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_APPEND_BUILT_ARGV
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-append-built-argv.py:12: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_OS_PATH_JOIN
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-os-path-join.py:9: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_JOINPATH
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-joinpath.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_PARTIAL_ARGV
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-partial-argv.py:10: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_SEMICOLON_SHARED_WAIVER
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-semicolon-shared-waiver.py:9: call invokes THIS CHECKOUT'S OWN `autoharn` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_OPAQUE_BEFORE_SENSITIVE
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-opaque-before-sensitive.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_ALIAS_APPEND
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-alias-append.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_BRANCH_ORDER_DISSOLVED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-branch-order-dissolved.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_CHAINED_ASSIGN
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-chained-assign.py:9: argv `y` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: chained-assign. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_TUPLE_UNPACK
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-tuple-unpack.py:9: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: tuple-unpack. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_FOR_TARGET
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-for-target.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: for-target. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_TUPLE_SWAP
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-tuple-swap.py:11: argv `a` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: tuple-unpack. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_WALRUS
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-walrus.py:9: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: walrus. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_BARE_LED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-bare-led.py:5: argv[0] is the BARE literal `'led'` naming this checkout's own `led` verb with NO REPO-path join at all (row 1249 fix-round 4, finding 4 -- the leak class's most literal spelling: relies on `cwd`/`PATH` resolution, which this gate does not attempt to prove or disprove). If `cwd` genuinely points at a scaffolded scratch copy, waive here naming that invariant. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_BARE_DOT_LED
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-bare-dot-led.py:5: argv[0] is the BARE literal `'./led'` naming this checkout's own `led` verb with NO REPO-path join at all (row 1249 fix-round 4, finding 4 -- the leak class's most literal spelling: relies on `cwd`/`PATH` resolution, which this gate does not attempt to prove or disprove). If `cwd` genuinely points at a scaffolded scratch copy, waive here naming that invariant. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_GETATTR_APPEND
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-getattr-append.py:9: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: dynamic-mutator. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_GLOBALS_APPEND
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-globals-append.py:11: argv `cmd` is repo-verb-bearing (resolves to one of this repo's own operator-verb paths at some point) but its dataflow is not statically provable -- rebound more than once, mutated (`.append`/`.extend`/`.insert`/subscript/slice/`del`/`+=`/`.remove`/`.pop`/`.sort`/`.reverse`/`.clear`), aliased (`x = y`, including a tuple-swap), or passed as a bare argument to another call anywhere in this file -- argv dataflow not statically provable, so this gate refuses rather than trust a partial or stale reconstruction (row 1249 fix-round 3/4: PROVE-OR-REFUSE, not better simulation). This name was also touched by a round-4 census-swept binding construct this gate cannot model precisely: dynamic-mutator. Use a direct inline literal argv at this call site, or waive here naming the reviewed invariant that makes it safe. Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_STARRED_LITERAL
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-starred-literal.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_IFEXP_JOIN
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-ifexp-join.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: RED_FSTRING_STR_CALL
  /tmp/fixture-deployment-pin-guard-kccjbldb/red-fstring-str-call.py:8: call invokes THIS CHECKOUT'S OWN `led` (a REPO-rooted path, or a constant bound to one) instead of a scaffolded scratch copy -- root-level verb shims and libexec/autoharn/<verb> resolve deployment from their own file location, never a caller's env override (row 1249; the row-1237-1244/1248 leak class). Waive with `fixture-scratch-pinning-guard-waiver: <reason>` if proven safe.
GREEN ok: GREEN_MUTATED_NON_SENSITIVE

ALL CASES OK -- fixture-deployment-pin-guard both polarities, zero residue

