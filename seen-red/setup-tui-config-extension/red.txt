seen-red/setup-tui-config-extension -- banked both-polarity evidence (fixture-census)

Work item setup-tui-config-extension (ledger row 685's audit / row 693): the audit's seven
coverage gaps closed in tools/setup_tui/, config-as-data idiom:

  1. identity_enforcement (hub-wide default) -- ChoiceField in the Boundary step, default
     "grace"; written to boundary-multiplex.toml ONLY when non-default.
  2. identity_enforcement per-deployment override -- optional ChoiceField, written into
     [deployments.NAME] only when set away from "inherit".
  3. log_level -- ChoiceField whose vocabulary is IMPORTED from
     serving/boundary_diagnostic_log.LEVELS (ADR-0012 P1), never a hardcoded copy.
  4/5. sse_poll_interval_secs (float, default 2.0, (0, 3600]) and max_sse_clients (int, default
     16, [1, 100000]) -- written only when non-default; bounds imported from
     serving/boundary_multiplex_config.py (renamed public, this build) rather than duplicated.
  6. courier configuration -- a new "Courier" section (tools/setup_tui/steps_courier.py): self/
     self_base DERIVED (read-only, ADR-0019 C2) from Birth's world name and Boundary's picked
     boundary_url; counterparts a ListField (world name -> base URL); authn fixed at
     "single-operator" (row 1162's own named empty slot), never a choice. Zero counterparts ->
     courier.toml is NOT written this run (libexec/autoharn/courier's own load_courier_config
     refuses an empty [courier.counterparts] table just as loudly as a missing file, so a stub
     would never be a working config -- disclosed in steps_courier.py's own submit() docstring).
  7. birth_stamp_secret -- a new feature_facts.toml entry documenting the auto-provisioned
     .claude/secrets/stamp_secret.hex (chmod 600, gitignored, birth-sequence provenance),
     rendered by steps_rehearsal_birth.py's own birth_submit.

Plus: config_schema.toml gains the corresponding closed keys (boundary.log_level/
identity_enforcement/identity_enforcement_override/sse_poll_interval_secs/max_sse_clients,
courier.counterparts) so --from-config round-trips every new field; a typo'd key is refused,
naming it. Forward slots (authn_mode, scope posture) are named as a comment in
config_schema.toml, not built as fields (neither spec is ratified).

WHAT THE FIXTURE PROVES, IN ORDER (seen-red/setup-tui-config-extension/run_fixtures.py):

  a. DEFAULTS-UNCHANGED REGRESSION -- every new field left at its own default emits
     BYTE-IDENTICAL boundary-multiplex.toml to before this build (no top-level keys at all,
     `[deployments.NAME]` unchanged); courier.toml is not written at all with zero counterparts.
  b. BOUNDARY NEW FIELDS THROUGH THE REAL LOADER -- non-default values for all five fields
     parse through serving.boundary_multiplex_config.load_multiplex_config_with_deployment_
     identity (the REAL loader, no synthetic parser) with every value intact, INCLUDING the
     per-deployment override differing from the hub-wide default. RED control: an out-of-range
     sse_poll_interval_secs ("0") is refused by steps_boundary.submit itself, naming the field.
  c. COURIER THROUGH THE REAL LOADER -- one counterpart parses through libexec/autoharn/
     courier's own real load_courier_config with self/self_base/authn/counterparts intact. RED
     control: a counterpart named the same as this world's own name is refused, naming the field.
  d. CONFIG-SCHEMA ROUND-TRIP -- render_toml -> load_config_file -> validate is a fixed point
     for every new key (green control). RED control: a typo'd key ("boundary.log_leveel") is
     refused, naming it.
  e. PILOT-DRIVEN END-TO-END -- the REAL Tree+Form app (tools.setup_tui.tui_app, Textual Pilot/
     run_test()) drives three real RadioSet clicks (log_level/identity_enforcement/
     identity_enforcement_override) and two real typed Input fields (sse_poll_interval_secs/
     max_sse_clients) in the real "boundary" section, then reads the persisted live-field slot
     back via fields.get_field_value and confirms steps_boundary.submit resolves the exact same
     values; then drives the real Add-modal flow (a real Add-button click, real typed modal
     Input fields, a real Save-button click -- the same AddItemModal flow seen-red/setup-tui-
     configtree-journey's own case 24 already proved for a MasterDetailFieldWidget, exercised
     here against a plain ListFieldWidget instead) in the real "courier" section, and confirms
     steps_courier.submit reaches the added row.

ONE DEFECT THIS FIXTURE'S OWN BUILD CAUGHT (named per this project's own "claims carry
witnesses" discipline): the first draft of case (e) read the outer `state` dict after driving
Pilot interactions and found every field still at its DEFAULT value, even though the widgets
themselves visibly changed (RadioSet.pressed_index moved, Input.value changed) -- root cause:
`tools.configtree.app.ConfigTreeApp.__init__` shallow-copies its `initial_state`
(`self.state = dict(initial_state or {})`), so a NEW top-level key (`"_live_fields"`) created
by the running app's own write-through lands on `app.state`'s OWN dict, invisible from the
fixture's outer `state` variable -- the exact silent-divergence class ADR-0012 P1 warns about,
here between two dict OBJECTS rather than two functions. FIXED (not by reading `app.state`
instead, which would only move the coupling, but by pre-seeding `state["_live_fields"] = {}`
before `build_app` -- the shallow copy then shares that ONE nested dict object by reference, the
same trick seen-red/setup-tui-config-file's own case v already relies on for its own overrides
dict, now named explicitly here for the next fixture author who hits this).

Full transcript (live run, this build):

case a ok: every new boundary field left at its own default emits BYTE-IDENTICAL
boundary-multiplex.toml to before this build (no top-level keys at all)
case a ok (courier): zero counterparts entered -- courier.toml is NOT queued for writing at all
case b ok: boundary-multiplex.toml with all five new fields set to non-default values parses
through the REAL serving.boundary_multiplex_config loader with every value intact -- including
the per-deployment override ('grace') differing from the hub-wide default ('enforce')
case b ok (RED control): an out-of-range sse_poll_interval_secs ('0') is refused by
steps_boundary.submit itself, naming the field
case c ok: courier.toml with one counterpart parses through the REAL
libexec/autoharn/courier load_courier_config with self/self_base/counterparts intact
({'self': 'cfgextc', 'self_base': 'http://127.0.0.1:8421', 'counterparts':
{'cfgextcpeer': 'http://127.0.0.1:8500'}})
case c ok (RED control): a counterpart named the same as this world's own name is refused by
steps_courier.submit, naming the field
case d ok: every new key (boundary.log_level/identity_enforcement/
identity_enforcement_override/sse_poll_interval_secs/max_sse_clients, courier.counterparts)
round-trips through render_toml -> load_config_file -> validate as a fixed point
case d ok (RED control): a typo'd key ('boundary.log_leveel') is refused, named in the refusal
case e ok (boundary): three real RadioSet clicks + two real typed Inputs thread through
get_field_value into the exact answers dict steps_boundary.submit resolves, matching the widget
state end to end
case e ok (courier): a real Add-button click + real typed modal Input fields + real Save-button
click lands a counterpart row through the SAME AddItemModal a plain ListField uses, and
steps_courier.submit reaches it
ALL CASES OK (or honestly UNEXERCISED) -- setup-tui-config-extension (ledger row 685's audit /
row 693), zero residue

Cases (a)-(d) need no live Postgres/GPG/boundary-service (every case drives `submit` directly or
via Pilot against synthetic-but-real state, never a live birth), so nothing here is UNEXERCISED
for lack of HARNESS_PGHOST/EPISTEMIC_PGHOST; case (e) is UNEXERCISED only if `textual` is not
importable on the running interpreter (witnessed importable, 8.2.8, this build).

ADDENDUM (fix round, review row 776 -- case f, `case_typed_construction`): each of the five NEW
boundary-multiplex values' typed homes in `boundary_config_values.py` gets one RED (out-of-
contract value refused at construction) + one GREEN (valid value round-trips) leg -- LogLevel,
IdentityEnforcementPosture, IdentityEnforcementOverride, SsePollIntervalSecs, MaxSseClients.

FURTHER ADDENDUM (work item setup-tui-bare-types-retrofit, ledger rows 778/789 -- case f
(retrofit), `case_typed_construction_retrofit`): the module's PRE-EXISTING bare scalars
(host/db/schema/kern/role, travelling bare through `steps_boundary.py`/`steps_substrate.py`/
`steps_rehearsal_birth.py`/`signed_genesis.py`/`principals_authority.py`) now get the SAME
treatment via five new typed homes in `tools/setup_tui/idtypes.py`'s "PG* TYPES" section --
PgHost, PgDatabase, PgSchema, PgKernSchema, PgRole -- each a thin frozen dataclass over
`probes.valid_hostname`/`probes.valid_identifier` (contract imported from `probes.py`, never
copied), one RED (out-of-contract value refused, naming the contract) + one GREEN
(round-trips through `.value`/`str()`) leg per home, same case-f pattern.

Full transcript (live run, this build, retrofit addendum):

case f ok: each of the five typed homes in boundary_config_values.py refuses an out-of-contract
value at construction (naming the contract) and round-trips a valid one -- LogLevel,
IdentityEnforcementPosture, IdentityEnforcementOverride, SsePollIntervalSecs, MaxSseClients
case f (retrofit) ok: each of the five NEW typed homes in idtypes.py's "PG* TYPES" section
refuses an out-of-contract value at construction (naming the contract) and round-trips a valid
one -- PgHost, PgDatabase, PgSchema, PgKernSchema, PgRole
ALL CASES OK (or honestly UNEXERCISED) -- setup-tui-config-extension (ledger row 685's audit /
row 693), zero residue

The consuming modules' own construction sites (not this fixture's own direct case f/f-retrofit
calls) are witnessed by the OTHER setup_tui fixture families still passing unmodified after this
retrofit: setup-tui-config-extension's own cases (a)-(e) above (steps_boundary.py's five
PRE-EXISTING host/db/schema/kern/role values, now PgHost/PgDatabase/PgRole/PgSchema/
PgKernSchema-typed, still resolve byte-identical boundary-multiplex.toml/plan argv), plus
setup-tui-signed-genesis-resume/setup-tui-signed-genesis-key-pinning (signed_genesis.py's
`_validated_dep_fields`, now PgSchema/PgRole/PgKernSchema-typed) and setup-tui-class-vocabulary-
drift (principals_authority.py's own near-identical `_validated_dep_fields`, now PgSchema/
PgRole-typed) -- all re-run green after this retrofit, live transcripts kept in this build's own
commit message rather than re-pasted here.

ADDENDUM (review row 832, fresh-context review of the retrofit above): the review found ONE
remaining birth site the retrofit missed -- `tools/setup_tui/app.py`'s `_run_from_config` built
bare host/db strings via `str()` straight from the config document and passed them to
`config_seam.check_world_and_dest` before any typed construction, unlike every sibling call site
(`steps_substrate.py`'s own `submit`). Fixed at the exact site (the `host = str(...)`/`db =
str(...)` lines immediately above the `check_world_and_dest` call): both now construct through
`idtypes.PgHost.parse`/`PgDatabase.parse` first, unwrapping to `str()` for the same downstream
call, matching the sibling idiom exactly. New leg `case_from_config_typed_construction_site`
added to this file.

RED (pre-fix, `tools/setup_tui/app.py` stashed back to review row 832's own defect, live run):

    AssertionError: check_world_and_dest was reached with a host that should have been refused
    at construction, before this call

    (raised by a stub installed in place of config_seam.check_world_and_dest that raises if
    called at all -- proving the pre-fix code reached the live-Postgres-probing call with an
    invalid host 'evil;host' instead of refusing at construction)

GREEN (post-fix, live run, full family):

case f ok: each of the five typed homes in boundary_config_values.py refuses an out-of-contract
value at construction (naming the contract) and round-trips a valid one -- LogLevel,
IdentityEnforcementPosture, IdentityEnforcementOverride, SsePollIntervalSecs, MaxSseClients
case f (retrofit) ok: each of the five NEW typed homes in idtypes.py's "PG* TYPES" section
refuses an out-of-contract value at construction (naming the contract) and round-trips a valid
one -- PgHost, PgDatabase, PgSchema, PgKernSchema, PgRole
case (review row 832) ok: app.py's _run_from_config now constructs host/db through
idtypes.PgHost/PgDatabase before calling config_seam.check_world_and_dest -- an invalid host
REFUSES at construction (naming probes.valid_hostname's contract) without ever reaching
check_world_and_dest / attempting a live Postgres probe, and a valid host/db reaches
check_world_and_dest with the byte-identical plain strings the config file named
ALL CASES OK (or honestly UNEXERCISED) -- setup-tui-config-extension (ledger row 685's audit /
row 693), zero residue

Gates re-run clean after this change: setup_tui_purity_gate, no_lazy_imports, max_lines,
fixture_census (all rc=0).
