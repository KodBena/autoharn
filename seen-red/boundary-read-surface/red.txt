boundary-read-surface -- banked RED evidence (2026-07-18, toy db 192.168.122.1, scratch worlds
wrfx<pid> plus this repo's own live autoharn1 world; runnable fixture:
seen-red/boundary-read-surface/run_fixtures.py, registered in gates/fixture_census.py).
Witnesses design/FABLE-BOUNDARY-READ-SURFACE-SPEC.md's WR1-WR5.

Two genuine RED->GREEN transitions this build's own live testing captured, both against real
infra, neither fabricated:

1. THE COLUMN-SHAPE GAP (a real production bug in the /views/{view} route this spec's own build
   introduced, found by testing against this repo's OWN live autoharn1 world -- lineage head
   s30, well short of s37). `VIEW_REGISTRY["work_item_violations"]` was first authored keyed on
   `target_id` (added only at kernel/lineage/s37-violation-disposition.sql). Object-existence
   detection (`_regclass_exists`) alone passed -- the VIEW exists on a pre-s37 world, just
   without that column -- so the route reached a bare SQL error:

     boundary_service: UNCLASSIFIED FAILURE (GET /d/autoharn1/views/work_item_violations):
     psql query failed (exit 3, ...): psql:/dev/stdin:3: ERROR:  column "target_id" does not
     exist
     LINE 1: ...ELECT * FROM autoharn1.work_item_violations WHERE target_id ...
     INFO: "GET /d/autoharn1/views/work_item_violations?after_id=0&limit=1000 HTTP/1.1" 500
     Internal Server Error

   This was a TYPED failure (`unclassified_failure`, never a bare 500 body) but a needlessly
   coarse one -- and it also diverged from the legacy tool: `legacy-led.tmpl`'s own `led work
   violations` query (`SELECT slug FROM work_item_violations`) never references target_id at
   all, so it read this SAME pre-s37 world cleanly (0 violations, a legitimate answer under that
   view's OLDER shape) while the served route errored. Fixed two ways: (a) `VIEW_REGISTRY`'s key
   column re-chosen to `slug` (present on every lineage shape this view has ever had, since s22
   -- the SAME column `work_review_gap`/`work_item_current` already key on); (b) a NEW,
   finer-grained capability check, `_column_exists` (serving/boundary_service.py), added
   alongside `_regclass_exists` -- object existence is not sufficient capability detection for a
   view whose COLUMN SET grew across lineage deltas while its NAME stayed stable. Re-verified
   GREEN against the SAME live autoharn1 world: `work-violations : UNAVAILABLE (This world's
   autoharn1.work_item_violations object exists but carries no 'target_id' column -- ...)` would
   have been the honest answer for a genuinely-target_id-dependent view; with the slug re-key,
   the route now serves this world's work_item_violations cleanly, matching the legacy tool's
   own reading exactly (both 0 violations on this world, WR1 verified equal on a fully s50-headed
   scratch world separately).

2. THE STALE-MODULE-PATH BUG (a fixture-harness bug, not a serving/ bug). WR3's own equality
   check imports `bootstrap/templates/asof-export.tmpl` by file path to call its
   `ledger_asof_rows` directly -- after this build renamed the ORIGINAL file to
   `legacy-asof-export.tmpl` and authored a NEW, served `asof-export.tmpl` under the old name,
   the fixture (still pointed at the now-served file) tried to call the SERVED module's
   `ledger_asof_rows(dep, ts)` with a legacy-shaped `dep` (a plain `DeploymentRecord`), which the
   served function expects to be a `ServedConfig` (needs `.base`):

     File ".../bootstrap/templates/asof-export.tmpl", line 108, in ledger_asof_rows
       rows = bcc.get_all_rows(cfg.base, f"/rows/asof/{encoded_ts}", cursor="after_id")
     AttributeError: 'DeploymentRecord' object has no attribute 'base'

   Fixed by repointing `ASOF_EXPORT_TMPL` to `legacy-asof-export.tmpl` explicitly (WR3's whole
   point is comparing the SERVED route against the LEGACY tool's own independent read -- it must
   never resolve to the file this same suite is testing). Re-run GREEN
   (`wr3-asof-equality-vs-legacy-nonvacuous`: legacy_n=16 served_n=16 row_sets_equal=True
   post_mid_row_id excluded from both).

Full GREEN transcript (same commit, same live toy db):

  == WR1: per-view row-set equality, served vs direct ==
  [11/11 views OK, row_sets_equal=True per view]
  == WR2: unknown view name -> typed 404 ==
  [ok] status=404 disposition=unknown_view, known=[11 views]
  == WR3: as-of reconstruction equality vs the legacy asof-export.tmpl read ==
  [ok] legacy_n=16 served_n=16 row_sets_equal=True post_mid_row_id excluded from both
  [ok] malformed ts -> typed 422 pre-kernel
  == WR4: GET /meta matches reality (view list + lineage head) ==
  [ok] known_views equals VIEW_REGISTRY; [ok] lineage_head equals migrate_core's own independent
  computation; [ok] boundary_version present
  == WR5: per-deployment saturation still fires on a /views/ route (WM4 method) ==
  [ok] burst_n=24 expected_per_dep_limit=12 deployment_saturated=12 statuses={503}

  ALL WR CHECKS OK

3. LEDGER ROWS 153/154 (2026-07-28, work items view-registry-decomposition-views /
   rows-bulk-superseded-read): the sixth VIEW_REGISTRY growth (work_edge_blocks_close,
   discharging_attest, work_violation_history, work_bookkeeping_closes, countersigned_in_force)
   plus GET /rows/current's new include_superseded opt-in. RED witnessed by stashing
   serving/boundary_service.py back to its pre-this-build committed state (git stash push --
   serving/boundary_service.py) while keeping THIS FILE'S OWN sibling fixture edits (the new
   birth_fixture_rows acts + WR6), then re-running (env -u PGOPTIONS python3 seen-red/
   boundary-read-surface/run_fixtures.py -- the env -u strips the session stamp hook's own
   PGOPTIONS, bound to this repo's autoharn3 deployment, which cannot validate against a scratch
   world's own kernel.stamp_secret):

     FAILURES: ['wr6b-include-superseded-true-marks-both-legibly',
                'wr6c-strict-parse-refusal-1', 'wr6c-strict-parse-refusal-TRUE',
                'wr6c-strict-parse-refusal-yes']

   wr6b failed with `superseded_row=None` (the pre-fix route has no `include_superseded` param at
   all, so the query string is silently ignored and the superseded row never appears -- exactly
   the structural exclusion the commission's own text names). wr6c's three legs each failed with
   `status=200` (the illegal spellings "1"/"TRUE"/"yes" were silently accepted as no-ops, never
   the typed 422 the fix adds). wr6a and wr6a' (explicit `include_superseded=false`) already
   passed pre-fix -- expected, since the default path is untouched by construction; their GREEN
   status pre-fix is not evidence of anything, only the ABSENCE of a false positive.

   The five new VIEW_REGISTRY members are NOT exercised by WR1 at all pre-fix (they are simply
   absent from `sorted(boundary_service.VIEW_REGISTRY)`, so the loop never reaches them) -- this
   is the RED state for Part 1 by construction (`VIEW_REGISTRY.get(view)` returns `None` for an
   unregistered name, and `views_view` refuses every such name with the SAME typed 404 WR2
   already banks evidence for), verified by reading `views_view`'s own dispatch rather than a
   second banked transcript.

   A PRE-EXISTING, UNRELATED HAZARD FOUND IN REACH while re-running the (stashed-back) baseline
   fixture unmodified: `CHAIN_FULL` (this file) stopped at s57, one delta short of s58/s59
   (design/FABLE-MISSIVES-KERNEL-SPEC.md's six missive_* views, already VIEW_REGISTRY members
   under an EARLIER, unrelated build) -- WR1's per-view loop 500'd reaching any missive_* member,
   even on the untouched baseline:

     RuntimeError: psql failed:  ERROR:  relation "wrfx<pid>.missive_delivery_audit" does not
     exist

   Fixed in reach (CLAUDE.md's engineering-responsibility rule -- this exact constant and this
   exact loop were already being touched by this build's own additions): `CHAIN_FULL` extended
   through s59. NOT carried further to the true s68 lineage head -- witnessed live that s61
   (design/FABLE-ENTITLEMENT-ENFORCEMENT-SPEC.md) adds a `key_binding_possession_ref`
   requirement this fixture's own possession-proof-free `principal_key_bound` birth act cannot
   satisfy without an unrelated signature ceremony:

     Ledger policy: a FRESH principal_key_bound row (principal_binding_active=true) must name a
     key_binding_possession_ref (s61, ...) -- this write supplies none.

   `bootstrap/migrate_core.py`'s `_current_head_and_missing` detects the lineage head PER WORLD
   (never against a hardcoded repo-wide constant), so stopping at s59 keeps WR4's served-vs-
   actual comparison internally honest -- confirmed by re-running WR4 GREEN below, not assumed.

   Full GREEN transcript (same commit -- serving/boundary_service.py restored, seen-red/
   boundary-read-surface/run_fixtures.py unchanged from the RED run above -- same live toy db,
   scratch world wrfx2969552, 44/44 checks ok, 0 FAIL):

     == scaffolding classic world wrfx2969552 (chain ends s59-missive-views.sql) ==
     == WR1: per-view row-set equality, served vs direct ==
     [ok] wr1-view-work_edge_blocks_close: status=200 served_n=1 direct_n=1 row_sets_equal=True
     [ok] wr1-view-discharging_attest: status=200 served_n=1 direct_n=1 row_sets_equal=True
     [ok] wr1-view-work_violation_history: status=200 served_n=1 direct_n=1 row_sets_equal=True
     [ok] wr1-view-work_bookkeeping_closes: status=200 served_n=1 direct_n=1 row_sets_equal=True
     [ok] wr1-view-countersigned_in_force: status=200 served_n=1 direct_n=1 row_sets_equal=True
     [every other VIEW_REGISTRY member OK too, 30/30 views total, row_sets_equal=True per view]
     == WR2: unknown view name -> typed 404 ==
     [ok] wr2-unknown-view-typed-404: status=404 disposition=unknown_view, known=30 views
          (alphabetical list includes all five new members plus the pre-existing 25)
     == WR6: GET /rows/current -- include_superseded opt-in, default unchanged ==
     [ok] wr6a-default-byte-identical-no-marking-field: status=200 served_n=27 direct_n=27
          row_sets_equal=True superseded_id=12 present=False no_is_current_field=True
     [ok] wr6a-explicit-false-identical-to-omitted: status=200 row_sets_equal_to_omitted=True
     [ok] wr6b-include-superseded-true-marks-both-legibly: status=200 superseded_row.id=12
          (kind=work_opened, is_current=False) superseding_row.id=14 (kind=note, supersedes=12,
          is_current=True) ground_truth_superseded_current='false' marking_matches_ground_truth=
          True
     [ok] wr6c-strict-parse-refusal-1/-TRUE/-yes: status=422 each, detail='include_superseded
          must be exactly "true" or "false" (or omitted, the default "false") -- strictly
          parsed, never coerced from another spelling; got <value>'

     ALL WR CHECKS OK (exit 0)

4. FIX ROUND (2026-07-28, coordinator fresh-context review of commit 6a104c0 -- one CRITICAL,
   one MODERATE, both addressed here). CRITICAL: `views_view`'s pre-fix-round pagination
   predicate (`WHERE key_col > cursor ORDER BY key_col LIMIT`) silently drops rows when a
   NON-unique key value straddles a page boundary -- live-reproduced on the reviewer's own two
   named instances (discharging_attest, work_violation_history) AND, per ADR-0000 Rule 2(a)'s
   "presumption inverted: check the universe outward," on a full re-audit of every OTHER
   VIEW_REGISTRY member: the two pre-existing members the reviewer named as sharing the SAME
   route code and defect (work_item_violations.slug, model_defeated_rows.attest_id), plus TWO
   FURTHER instances this audit found and empirically confirmed (review_gap.id,
   work_review_gap.slug) that the reviewer's own finding did not name. A seventh member,
   missive_stale.id, is classified non-unique on STRUCTURAL grounds only (its JOIN can fan one
   original send out across more than one matching reply) -- NOT empirically reproduced (this
   fix round's own uniqueness probe could not clear the s58 missive-world-identity birth
   ceremony in the time available); named honestly as UNEXERCISED rather than silently assumed
   either way.

   RED (`git stash push -- serving/boundary_service.py`, reverting to commit 6a104c0's own
   pre-fix-round state, keeping THIS fixture file's own new WR7/WR8 additions; re-run via
   `env -u PGOPTIONS python3 seen-red/boundary-read-surface/run_fixtures.py` -- the env -u
   strips the session stamp hook's own PGOPTIONS, bound to autoharn3, unable to validate
   against a scratch world's own kernel.stamp_secret):

     FAILURES: ['wr8a-after-tie-refused-on-unique-view',
                'wr8b-discharging_attest-lossless-limit1-walk',
                'wr8b-work_violation_history-lossless-limit1-walk',
                'wr8b-work_item_violations-lossless-limit1-walk',
                'wr8b-model_defeated_rows-lossless-limit1-walk',
                'wr8b-review_gap-lossless-limit1-walk',
                'wr8b-work_review_gap-lossless-limit1-walk']

   Each `wr8b` failure shows the exact data-loss shape (a constructed, genuine duplicate key
   value, limit=1 keyset walk, paginated_total < direct_total):

     wr8b-discharging_attest:       dup_value=29  direct_total=3 paginated_total=2 (LOST 1)
     wr8b-work_violation_history:   dup_value='dupfx-viol-...' direct_total=4 paginated_total=3 (LOST 1)
     wr8b-work_item_violations:     dup_value='dupfx-viol-...' direct_total=3 paginated_total=2 (LOST 1)
     wr8b-model_defeated_rows:      dup_value=42  direct_total=2 paginated_total=1 (LOST 1)
     wr8b-review_gap:               dup_value=44  direct_total=4 paginated_total=3 (LOST 1)
     wr8b-work_review_gap:          dup_value='dupfx-wrg-...' direct_total=4 paginated_total=3 (LOST 1)

   `wr8a-after-tie-refused-on-unique-view` failed too (pre-fix-round `after_tie` is not a
   recognized param at all, so it is silently ignored -- `status=200`, not the fix round's own
   typed 422).

   THE FIX: a per-row CONTENT tiebreaker -- `(key_col, md5(the view row::text))` -- applied
   ONLY to the seven now-non-unique VIEW_REGISTRY members (a new third element on every
   registry tuple, `key_unique: bool`); a UNIQUE-key view's own code path is left BYTE-FOR-BYTE
   UNTOUCHED (never routed through the new machinery), witnessed with a literal served-vs-direct
   diff on `work_item_current` carrying NO `_page_tie` field. The served response for a
   NON-unique view gains exactly one field per row, `_page_tie` (the client resupplies it as
   the new `after_tie` query param to walk past a repeated key value losslessly); `after_tie` is
   strictly typed (empty, or exactly 32 lowercase hex chars) and refused on a unique-key view or
   a malformed value, never silently ignored or guessed at.

   GREEN (same commit, serving/boundary_service.py restored; same live toy db, scratch world
   wrfx3037839 plus a SEPARATE minimal wrfxs683037839 for WR7; 54/54 checks ok, 0 FAIL):

     == WR8: composite-tiebreaker fix -- non-unique key pagination, limit=1, no loss ==
     [ok] wr8a-unique-view-byte-identical: status=200 served_n=5 direct_n=5 no_page_tie_field=True
     [ok] wr8a-after-tie-refused-on-unique-view: status=422, detail names after_tie and the
          view's own unique key
     [ok] wr8b-discharging_attest: dup_value=29 dup_count_direct=2 direct_total=3
          paginated_total=3 dup_count_paginated=2 row_sets_equal=True
     [ok] wr8b-work_violation_history: dup_count_direct=2 direct_total=4 paginated_total=4
          dup_count_paginated=2 row_sets_equal=True
     [ok] wr8b-work_item_violations: dup_count_direct=2 direct_total=3 paginated_total=3
          dup_count_paginated=2 row_sets_equal=True
     [ok] wr8b-model_defeated_rows: dup_value=42 dup_count_direct=2 direct_total=2
          paginated_total=2 dup_count_paginated=2 row_sets_equal=True
     [ok] wr8b-review_gap: dup_value=44 dup_count_direct=2 direct_total=4 paginated_total=4
          dup_count_paginated=2 row_sets_equal=True
     [ok] wr8b-work_review_gap: dup_count_direct=2 direct_total=4 paginated_total=4
          dup_count_paginated=2 row_sets_equal=True

   MODERATE: countersigned_in_force's true defining chain runs through s68 (+2 refusal-
   disposition columns, refusal_attempted_kind_disposition/refusal_attempted_actor_disposition)
   but the MAIN wrfx world's own CHAIN_FULL stops at s59 (the DOCUMENTED s61 key_binding_
   possession_ref blocker -- see CHAIN_FULL's own comment), so its WR1 GREEN validates a
   2-columns-short shape. Fixed by disclosure (VIEW_REGISTRY's own comment, this fixture file's
   own module docstring) PLUS a live witness rather than a silent gap: WR7, a SEPARATE, minimal,
   s68-headed scratch world (CHAIN_S68) that skips the ONE act (principal_key_bound) the s61
   blocker constrains, so the blocker never fires:

     == WR7: countersigned_in_force at the TRUE s68 lineage head (separate world) ==
     [ok] wr7-setup-server-healthy: GET /d/wrfxs683037839/health up=True
     [ok] wr7-countersigned-in-force-true-s68-shape: status=200 served_n=1 direct_n=1
          has_s68_cols=True (both refusal_attempted_kind_disposition and
          refusal_attempted_actor_disposition present per row)

   Full boundary-service family (W1-W36m) re-run clean after the fix, no regressions.
