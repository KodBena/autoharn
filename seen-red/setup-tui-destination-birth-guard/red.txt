seen-red/setup-tui-destination-birth-guard -- banked RED evidence (both-polarity proof, fixture-census)

design/FABLE-SETUP-TUI-DESTINATION-STATE-SPEC.md §5's third witness-set item, first closed
defect: "screen_birth accepting an unchecked dest (red against pinned pre-fix module)". Pins
tools/setup_tui/screens.py as it stood at commit a9d779f (this build's own base commit) via
`git show` + importlib (the same technique
seen-red/setup-tui-pure-core-foundation/run_fixtures.py's _load_pinned_commit_executor
established), drives screen_birth against a real on-disk FOREIGN scratch directory.

Real captured stdout, 2026-07-21, a live run against this commit's working tree:

$ python3 seen-red/setup-tui-destination-birth-guard/run_fixtures.py
==============================================================================
5/11 Birth
==============================================================================
Run the real birth now?: yes   [scripted]
World name: scratchworld-red   [scripted]
Project name (deployment.json 'name'): scratchworld-red   [scripted]
  $ /bootstrap/new-project.sh <scratch>/foreign_dest --new-world scratchworld-red --db toy
    --host 192.0.2.1 --name scratchworld-red
  (the real birth output, and the maintainer's own copy-paste signing line, stream at commit
  time -- this screen only queues the act.)
case RED ok: pre-fix screen_birth queues a birth plan entry against a FOREIGN destination, no
  acknowledgment, no refusal -- reproducing the closed defect

==============================================================================
5/11 Birth
==============================================================================
Run the real birth now?: yes   [scripted]
World name: scratchworld-green   [scripted]
Project name (deployment.json 'name'): scratchworld-green   [scripted]
  REFUSED: '<scratch>/foreign_dest' classifies as FOREIGN content (non-empty, no autoharn birth
  evidence; sample: ['README.md']) -- birth into non-autoharn content needs the explicit
  acknowledgment from fork-target's FOREIGN mode, never queued silently. Go back to
  fork-target, or pick a different destination.
case GREEN ok: current screen_birth refuses -- no birth entry queued, a REFUSED checklist row
  recorded instead
ALL CASES OK -- screen_birth's FOREIGN-destination gate, red before green, pinned pre-fix
module vs current, zero mocks

exit 0. Pure decision-phase call only -- no Postgres, gpg, or network touched (the pre-fix
module's own `_new_project_argv` command is printed, never executed, exactly as the current
pure-core architecture already keeps every screen a decider, not an actor).
