=== a-identity-normalizes-conversion-pair ===
  [ok] identity(work_open:item-x)='work:item-x' identity(work_review_deferred:item-x)='work:item-x' (equal=True); identity(work_open:item-y)='work:item-y' (differs=True); identity(review_gap:42)='review_gap:42' (untouched)

=== b-conversion-inherits-breaker ===
  [ok] raw_string_subset_alone=False (expect False -- proves the identity check is load-bearing) _breaker_transition(...)=2 (expect 2, inherited from prior count=1)

=== c-genuinely-new-debt-still-resets ===
  [ok] _breaker_transition(...)=1 (expect 1 -- a genuinely new slug's debt is not an identity the prior state names)

=== d-literal-shrinkage-still-inherits ===
  [ok] _breaker_transition(...)=4 (expect 4 -- the PRE-EXISTING raw-string strict-subset branch, unperturbed by this delta's addition)

ALL CASES OK -- hooks/stop_clean_exit.py s29 debt-type CONVERSION proof (identity normalizes the work_open<->work_review_deferred pair / a same-size type-converted debt set inherits the breaker / genuinely new debt still resets / pre-existing literal-shrinkage inheritance is unperturbed).
