Witnessed 2026-07-26: seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py genuinely goes
RED when the runtime-foreclosure mechanism itself (design/FABLE-FIXTURE-SANDBOX-RUNTIME-
FORECLOSURE-SPEC.md) is absent -- exactly the pre-build state, reproduced here rather than
asserted.

Reproduction: the built ./autoharn and libexec/autoharn/led were temporarily replaced with their
pre-build content (`git show HEAD:autoharn`, `git show HEAD:libexec/autoharn/led` -- HEAD at
build time, 0ffc8da, carries neither file's fixture-sandbox check), then this fixture was re-run
unmodified:

$ cp autoharn /tmp/autoharn.mine; cp libexec/autoharn/led /tmp/led.mine
$ git show HEAD:autoharn > autoharn; git show HEAD:libexec/autoharn/led > libexec/autoharn/led
$ chmod +x autoharn libexec/autoharn/led
$ python3 seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py
1-marker-refuses: FAIL -- exit=4 combined_tail="...LEDGER_DEPLOYMENT or PICKUP_DEPLOYMENT..."
1-marker-refuses names both sanctioned exits: FAIL -- teaching text should name both the
  scratch-world exit and the waiver
2a-os-system-refused: FAIL -- exit=4 ...
2b-keyword-argv-refused: FAIL -- exit=4 ...
2c-match-case-argv-refused: FAIL -- exit=4 ...
2d-alias-chain-refused: FAIL -- exit=4 ...
2e-direct-libexec-bypass-refused: FAIL -- exit=4 ...
3-waiver-proceeds-no-refusal: PASS -- exit=4 ...
3-waiver-reason-echoed: FAIL -- combined_tail="...LEDGER_DEPLOYMENT or PICKUP_DEPLOYMENT..."
4-empty-waiver-refused: FAIL -- exit=4 ...
=== 5-scratch-world-unaffected ===
  [UNEXERCISED] BLOCKED: neither HARNESS_PGHOST nor EPISTEMIC_PGHOST is set ...
6-census-red-without-marker: PASS -- exit=1 (expected 1)
6-census-green-with-marker: PASS -- exit=0 (expected 0)
FAILURES: ['1-marker-refuses', '1-marker-refuses names both sanctioned exits',
  '2a-os-system-refused', '2b-keyword-argv-refused', '2c-match-case-argv-refused',
  '2d-alias-chain-refused', '2e-direct-libexec-bypass-refused', '3-waiver-reason-echoed',
  '4-empty-waiver-refused']

Every case that depends on the runtime refusal (1, all five of case 2's evasion specimens, the
waiver-echo half of case 3, and case 4) goes RED the instant the mechanism is absent -- exit 4 is
this worktree's own "deployment record not found" refusal (there is no deployment.json here),
proving the pre-build dispatcher does not refuse under the marker at all; it just falls straight
through to the ordinary missing-deployment path. Case 6 (the census additive check, which reads
gates/fixture_census.py -- untouched by this revert) and case 3's "proceeds" half (also
untouched-dispatcher-shaped, since without any check at all the call trivially "proceeds") stay
green, exactly as expected: this reproduction isolates the runtime-refusal claim specifically,
not the whole file.

The dispatcher and led shim were restored to their built (post-fix) content immediately after
this capture:

$ cp /tmp/autoharn.mine autoharn; cp /tmp/led.mine libexec/autoharn/led; chmod +x autoharn libexec/autoharn/led
$ git diff --stat autoharn libexec/autoharn/led
 autoharn             | 28 ++++++++++++++++++++++++++++
 libexec/autoharn/led | 12 ++++++++++++
 2 files changed, 40 insertions(+)

-- byte-identical to the build's own committed content (confirmed via `git diff` against the
pre-restore working tree showing zero remaining delta once restored) -- and the fixture re-run
green afterward (see this build's own report for that transcript). Case 5 (the non-false-
positive leg) was separately exercised LIVE with HARNESS_PGHOST=192.168.122.1 against the built
(not reverted) code, since it requires the fix to be present, not absent, to prove anything; its
own transcript lives in the build report, not here.

--------------------------------------------------------------------------------------------

Appendix, 2026-07-26: case 7 (libexec/autoharn-service's --help exemption made strict two-token,
matching every other check site) added in response to a CLEARED-BUT-MODERATE-SILENT review
finding on this build. The pre-fix shape (`if not argv or argv[0] in ("--help", "-h"): ...
return` -- BEFORE fixture_sandbox.check()) let `AUTOHARN_FIXTURE_SANDBOX=1 python3 libexec/
autoharn-service --help start` print help and exit 0 without the sandbox check ever running; not
exploitable today (the early return precedes dispatch to any subcommand), but an undisclosed
inconsistency and a latent hazard next to the strict shape used everywhere else.

Reproduction: libexec/autoharn-service was temporarily replaced with its pre-fix content at the
commit this fix was built on top of (`git show c065485:libexec/autoharn-service`), then this
fixture was re-run unmodified:

$ cp libexec/autoharn-service /tmp/autoharn-service.mine
$ git show c065485:libexec/autoharn-service > libexec/autoharn-service; chmod +x libexec/autoharn-service
$ python3 seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py
7a-service-help-trailing-token-refused: FAIL -- exit=0 tail="...refuses, never guesses, otherwise"
7b-service-exact---help-still-prints-help: PASS -- exit=0 ...
7b-service-exact--h-still-prints-help: PASS -- exit=0 ...
7c-service-bare-argv-refused-under-marker: FAIL -- exit=0 tail="...refuses, never guesses, otherwise"
7d-no-marker-byte-identical-[...]: PASS (all six argv shapes) -- the NO-MARKER path was already
  identical pre-fix (expected: this leg diffs against this exact commit's own content, so it is
  a tautology before the fix and stays a live regression guard after)
FAILURES: [..., '7a-service-help-trailing-token-refused', '7c-service-bare-argv-refused-under-marker']

Cases 7a and 7c go RED exactly as expected: the pre-fix exemption's loose `argv[0] in (...)`
test matched `--help` regardless of trailing tokens (7a) and matched nothing for bare argv,
falling through to the same short-circuit via `not argv` (7c) -- both printed help and exited 0
under the marker instead of refusing, proving the pre-fix dispatcher's help path did not run the
sandbox check at all for either shape. 7b and 7d stay green, exactly as expected: 7b's shape
(exactly `--help`/`-h`) was never broken by the pre-fix code, and 7d diffs the NO-MARKER path
against this same pre-fix content, so it is definitionally identical before the fix.

libexec/autoharn-service was restored to its built (post-fix) content immediately after this
capture:

$ cp /tmp/autoharn-service.mine libexec/autoharn-service; chmod +x libexec/autoharn-service
$ git diff --stat libexec/autoharn-service
 libexec/autoharn-service | 22 +++++++++++++++++++++-
 1 file changed, 21 insertions(+), 1 deletion(-)

-- byte-identical to the fix's own committed content (zero remaining delta once restored) -- and
the fixture re-run green afterward, all 7a-7d cases PASS, no regression in cases 1-6.

--------------------------------------------------------------------------------------------

Dated note, 2026-07-26 (work item root-shim-pruning, ledger rows 1357/1358/1359): case 2d
(alias-chain-refused) is RETIRED, not repointed. Row 1357 (maintainer ruling) inverted the
umbrella spec's §6 removal schedule -- the ten root deprecation shims (`asof-export`,
`attest-tags`, `audit`, `distance-to-clean`, `doctor`, `judge`, `led`, `migrate`, `pickup`,
`verify-chain`; see design/FABLE-AUTOHARN-UMBRELLA-CLI-SPEC.md §6's dated amendment) are removed
BEFORE 2.0.0 as a precondition, not "at the first post-2.0.0 minor". `./led` -- the exact
one-hop indirection case 2d drove (`./led --recent 1`, execing
`./autoharn led "$@"` underneath) -- is one of the ten deleted in this build. The property case
2d witnessed (the runtime-foreclosure refusal surviving one hop of shim indirection before
reaching ./autoharn) is now VACUOUS: there is no `./led` left to hop through, and no other
root-level file left that has this alias-chain shape (`courier`, `extract-context`, `orchlog`,
`otel-attest`, `otel-watch` are the five root executables that survive this pruning, and none
of them exec `./autoharn <verb> "$@"` -- each is a standalone verb by its own documented design,
never an alias into the umbrella dispatcher). No repoint target exists, so the case is retired
outright rather than repointed to a shape that doesn't test the same thing. Case 2e (direct
libexec bypass -- skipping ./autoharn's own dispatch and landing straight on
libexec/autoharn/led) is UNCHANGED and continues to cover the other indirection direction (going
straight to the target instead of through an alias); libexec/autoharn/led itself is untouched by
this pruning (only the repo-root alias that pointed at it is gone). Fixture re-run after the
retirement (case 2 now has four specimens, a/b/c/e, not five):

$ python3 seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py
(see this build's own report for the full transcript; case 1, case 2's remaining four
specimens, cases 3/4/6/7 all green, case 5 UNEXERCISED as before with the same named
HARNESS_PGHOST/EPISTEMIC_PGHOST blocker.)
