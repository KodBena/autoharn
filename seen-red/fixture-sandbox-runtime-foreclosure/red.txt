Witnessed 2026-07-26: seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py genuinely goes
RED when the runtime-foreclosure mechanism itself (design/FABLE-FIXTURE-SANDBOX-RUNTIME-
FORECLOSURE-SPEC.md) is absent -- exactly the pre-build state, reproduced here rather than
asserted.

Reproduction: the built ./autoharn and libexec/autoharn/led were temporarily replaced with their
pre-build content (`git show HEAD:autoharn`, `git show HEAD:libexec/autoharn/led` -- HEAD at
build time, 0ffc8da, carries neither file's fixture-sandbox check), then this fixture was re-run
unmodified:

$ cp autoharn /tmp/autoharn.mine; cp libexec/autoharn/led /tmp/led.mine
$ git show HEAD:autoharn > autoharn; git show HEAD:libexec/autoharn/led > libexec/autoharn/led
$ chmod +x autoharn libexec/autoharn/led
$ python3 seen-red/fixture-sandbox-runtime-foreclosure/run_fixtures.py
1-marker-refuses: FAIL -- exit=4 combined_tail="...LEDGER_DEPLOYMENT or PICKUP_DEPLOYMENT..."
1-marker-refuses names both sanctioned exits: FAIL -- teaching text should name both the
  scratch-world exit and the waiver
2a-os-system-refused: FAIL -- exit=4 ...
2b-keyword-argv-refused: FAIL -- exit=4 ...
2c-match-case-argv-refused: FAIL -- exit=4 ...
2d-alias-chain-refused: FAIL -- exit=4 ...
2e-direct-libexec-bypass-refused: FAIL -- exit=4 ...
3-waiver-proceeds-no-refusal: PASS -- exit=4 ...
3-waiver-reason-echoed: FAIL -- combined_tail="...LEDGER_DEPLOYMENT or PICKUP_DEPLOYMENT..."
4-empty-waiver-refused: FAIL -- exit=4 ...
=== 5-scratch-world-unaffected ===
  [UNEXERCISED] BLOCKED: neither HARNESS_PGHOST nor EPISTEMIC_PGHOST is set ...
6-census-red-without-marker: PASS -- exit=1 (expected 1)
6-census-green-with-marker: PASS -- exit=0 (expected 0)
FAILURES: ['1-marker-refuses', '1-marker-refuses names both sanctioned exits',
  '2a-os-system-refused', '2b-keyword-argv-refused', '2c-match-case-argv-refused',
  '2d-alias-chain-refused', '2e-direct-libexec-bypass-refused', '3-waiver-reason-echoed',
  '4-empty-waiver-refused']

Every case that depends on the runtime refusal (1, all five of case 2's evasion specimens, the
waiver-echo half of case 3, and case 4) goes RED the instant the mechanism is absent -- exit 4 is
this worktree's own "deployment record not found" refusal (there is no deployment.json here),
proving the pre-build dispatcher does not refuse under the marker at all; it just falls straight
through to the ordinary missing-deployment path. Case 6 (the census additive check, which reads
gates/fixture_census.py -- untouched by this revert) and case 3's "proceeds" half (also
untouched-dispatcher-shaped, since without any check at all the call trivially "proceeds") stay
green, exactly as expected: this reproduction isolates the runtime-refusal claim specifically,
not the whole file.

The dispatcher and led shim were restored to their built (post-fix) content immediately after
this capture:

$ cp /tmp/autoharn.mine autoharn; cp /tmp/led.mine libexec/autoharn/led; chmod +x autoharn libexec/autoharn/led
$ git diff --stat autoharn libexec/autoharn/led
 autoharn             | 28 ++++++++++++++++++++++++++++
 libexec/autoharn/led | 12 ++++++++++++
 2 files changed, 40 insertions(+)

-- byte-identical to the build's own committed content (confirmed via `git diff` against the
pre-restore working tree showing zero remaining delta once restored) -- and the fixture re-run
green afterward (see this build's own report for that transcript). Case 5 (the non-false-
positive leg) was separately exercised LIVE with HARNESS_PGHOST=192.168.122.1 against the built
(not reverted) code, since it requires the fix to be present, not absent, to prove anything; its
own transcript lives in the build report, not here.
