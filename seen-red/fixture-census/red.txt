fixture-census: 22 breach(es) — the seen-red corpus is not intact:

  !! ORPHANED seen-red dir: seen-red/04-consumer-no-vacuous-pass/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/05-verify-adapter/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/06-append-only-integrity/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/09-relevant-act-classification/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/12-contemporaneity-degrade/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/18-bash-write-classification/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/24-destructive-ddl-guard/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/25-operator-turn-extraction/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/31-interception-stamp/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/33-staging-guard/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/35-delivery-freight-integrity/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/36-consumer-substrate-required/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/38-review-without-detail/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/39-binder-bind-many/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/42-gate-journal-registered/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/43-arming-delivery-set/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/45-criterion-reviewer-grants/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/engine-inc1-controls/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/fixture-census/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/layout-census/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/review-fixpoint/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
  !! ORPHANED seen-red dir: seen-red/s19-trigger-search-path/ has no registry entry (a gate's proof with no owning gate — register it or remove it)
# fixture-census red-specimen: exit=1 (expect 1 — RED on an empty registry)
