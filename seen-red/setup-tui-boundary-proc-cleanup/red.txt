seen-red/setup-tui-boundary-proc-cleanup -- banked RED evidence (both-polarity proof, fixture-census)

Ledger row 1799 finding 6: tools/setup_tui/app.py's screen-driving loop did not guarantee
`_terminate_boundary_proc` on an ORDINARY uncaught exception from a screen function (only the two
typed exits, ScriptExhausted/KeyboardInterrupt, ran cleanup) -- a real boundary_service child
process this run started could be orphaned. This fixture drives the REAL app.main() twice against
a real, live boundary_service child, loading `git show HEAD:tools/setup_tui/app.py` (this
worktree's OWN pre-fix commit) for the RED leg and the current, fixed app.py for the GREEN leg,
with an injected ordinary RuntimeError in the screen immediately after boundary start.

Real captured stdout, 2026-07-19, a live run against this commit's tree and its own HEAD
(zero residue after cleanup, exit 0):

RED ok (pre-fix, HEAD:tools/setup_tui/app.py, no try/finally): boundary_service ORPHANED after the injected exception -- live pid(s) [2920842] observed via ps
GREEN ok (post-fix, current tools/setup_tui/app.py, try/finally): boundary_service REAPED -- zero live pid after the SAME injected exception, app.py's own cleanup message observed
ALL CASES OK -- app.py boundary_proc cleanup guaranteed on an ordinary uncaught exception, red before green, live infra, zero residue
