seen-red/ac-boundary-scope-filter/red.txt -- RED capture, banked per gates/fixture_census.py's
own "a gate never seen red is a claim" discipline (ADR-0011).

Captured by running seen-red/ac-boundary-scope-filter/run_fixtures.py (this fixture's own code,
unchanged) against the tree BEFORE this build's own changes: `git stash push -u` of
serving/boundary_scope_filter.py (the new module), serving/boundary_service.py,
serving/boundary_diagnostic_log.py, serving/boundary_models.py, serving/boundary_read_journal.py
-- this fixture's own directory (seen-red/ac-boundary-scope-filter/) and
gates/fixture_census.py's own registry entry left OUT of the stash, so the proof itself and its
registration both stayed in place; only the production code they exercise reverted.

Command: env -u PGOPTIONS HARNESS_PGHOST=192.168.122.1 python3 seen-red/ac-boundary-scope-filter/run_fixtures.py

Observed (verbatim):

Traceback (most recent call last):
  File ".../seen-red/ac-boundary-scope-filter/run_fixtures.py", line 67, in <module>
    import boundary_scope_filter  # noqa: E402  (this build's own new module -- unit legs call it directly)
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
ModuleNotFoundError: No module named 'boundary_scope_filter'

Exit code: 1

WHY THIS IS THE RIGHT RED (not an accidental/unrelated failure): design/
FABLE-ACCESS-CONTROL-AND-INFORMATION-FLOW-SPEC.md §1b/§1c/§2/§4's own boundary-side scope
enforcement filter (serving/boundary_scope_filter.py) does not exist at all on the pre-build
tree -- this fixture's own unit legs call `boundary_scope_filter.apply_scope` directly, so the
import itself is the first thing to fail, before a single HTTP request is even made (and before
any of the live scratch-world legs, which would ALSO have failed differently: on the pre-build
tree, `serving/boundary_service.py`'s `_json_read_response` never threads `cfg`/`view`/
`id_field` through at all, so every scoped-principal leg below would instead observe the
scope-bind write succeed silently but every read stay UNFILTERED -- the exact vulnerability
this build closes, not merely a missing import).

After the build, the SAME fixture, unchanged, passes every one of its witness legs GREEN:
  - 5 unit legs (disclosure-mode default, thread-family match + full-tier drop, the unknown-
    exclusion-family loud refusal, vendor/anonymous always-passthrough) against
    `boundary_scope_filter.apply_scope` directly, no live substrate needed.
  - leg1 unarmed-world-byte-identical (reviewer's own read == anonymous's own read, before any
    scope is ever bound).
  - leg2 kind-class exclusion, marked tier (content withheld, typed marker kept) + the
    unscoped-caller-unchanged control.
  - leg3 hash_stub tier (content withheld, row_hash disclosed and verified equal to the real
    row's own hash).
  - leg4 full tier (the row absent from the list route with no marker at all, scope-relative
    count; the single-row route answers the SAME 404 shape as a genuinely absent row).
  - leg5 rows-family exclusion (an explicit row id).
  - leg6 work-item-lineage exclusion on GET /work/items (an exact work_slug).
  - leg7 the read journal carries a typed redaction summary (family/value/disclosure_mode/
    count), never the excluded row's own statement text.
  - leg8/leg9 /health's capabilities.s70_scope_filter is true on the s70-headed world, false
    (and every route byte-identical regardless of identity) on a pre-s70 world.

Witnessed against two real scratch worlds (acsfw<pid>, s70-headed via CHAIN_S70; acsfp<pid>,
pre-s70 via CHAIN_B) on dynamic loopback ports, torn down with zero residue on every run
(confirmed: no acsf*-prefixed schema survives in db=toy after either a pass or a raised
exception, `finally` always tears down).

===============================================================================================
FIX ROUND (adjudication row 889, closing a fresh-context review's BLOCKS on commit 4cf16621):
new RED capture for legs 6b/6c/6d and the new unit leg, banked the SAME way -- `git stash push
-u -- serving/boundary_scope_filter.py serving/boundary_service.py` (this fixture's own
directory left OUT of the stash, so the UPDATED proof runs against the PRE-fix-round production
code), run, then `git stash pop` to restore the fix.

Command: env -u PGOPTIONS HARNESS_PGHOST=192.168.122.1 python3 seen-red/ac-boundary-scope-filter/run_fixtures.py

Observed (verbatim, trimmed of the full served-row dicts for readability -- the FAIL lines
themselves, unedited):

=== unit-surface-not-granted-denies-whole-route (THE CRITICAL, in-process) ===
  [FAIL] an allow-list binding that never granted 'ledger_current' must deny EVERY row on that
  surface, not silently no-op past an empty scope_exclusions -- got content=[{'id': 1, 'kind':
  'note'}, {'id': 2, 'kind': 'finding'}] redactions=[]

=== leg6b-non-listed-surface-fully-redacted ===
  [FAIL] fourth-reviewer (granted ONLY work_item_current, no exclusions) reads /rows/current
  (ledger_current, NOT granted) -- every row must be redacted as surface-not-granted, none may
  carry its own statement text; got [... every row served IN FULL, unredacted, including
  'statement': 'author registered (fixture genesis exception)' and 'principal_subject': 1 ...]

=== leg6c-bare-arming-denies-every-route ===
  [FAIL] fifth-reviewer (armed, NULL scope_surfaces, no exclusions) reads /rows/current -- every
  row must be redacted, none may leak its own statement text; got [... same full, unredacted
  content as leg6b's FAIL above ...]

=== leg6d-full-tier-body-matches-genuine-absence-template ===
  [FAIL] GET /rows/{finding_id} under full-tier exclusion must render EXACTLY row_by_id's own
  genuine-absence template for this id -- got {'detail': "no row visible to this principal's
  current scope at this route (design/FABLE-ACCESS-CONTROL-AND-INFORMATION-FLOW-SPEC.md sec1c:
  this row's disclosure tier is 'full' -- existence itself is withheld, indistinguishable from a
  row that never existed at all)."}

4 FAILURE(S): unit-surface-not-granted-denies-whole-route, leg6b-non-listed-surface-fully-
redacted, leg6c-bare-arming-denies-every-route, leg6d-full-tier-body-matches-genuine-absence-
template

WHY THIS IS THE RIGHT RED: the first three are THE CRITICAL this fix round closes, reproduced
exactly as the fresh-context review described it -- `resolve_scope` fetched `scope_surfaces`
and no code path ever consulted it, so an armed binding with an allow-list and empty row
exclusions (leg6b), or with NO allow-list bound at all (leg6c), fell through to the pre-fix
`if not exclusions: return no_op` early return and served EVERY row unredacted, statement text
and all -- the exact fail-open leak. The fourth is the MODERATE this fix round also closes: the
pre-fix full-tier 404 body ("no row visible to this principal's current scope...") is NOT
byte-identical to `row_by_id`'s own genuine-absence body ("no row {id}"), so a client comparing
response bodies could distinguish "scoped out" from "genuinely absent" through the message text
alone. Every OTHER leg (1-9, including the pre-existing units re-armed with an explicit
`scope_surfaces` this fix round now requires) stayed GREEN against the reverted production
code, confirming those legs' own red/green history is undisturbed by this round's changes.

After restoring the fix (`git stash pop`), the SAME fixture, unchanged, passes every leg GREEN,
including the four above and the new leg6e timing disclosure (printed, not asserted -- see
serving/boundary_scope_filter.py's own module docstring, "DISCLOSED RESIDUAL" / "HOT-PATH
COST", for the measured numbers this run produced).
