seen-red/setup-tui-purity-gate -- banked RED evidence (both-polarity proof, fixture-census)

design/FABLE-SETUP-TUI-REBUILD-SPEC.md §2/§6 (2026-07-22 wholesale rebuild): the gate was
REWRITTEN for the tools/configtree library split -- two AST-level invariants (no print/input/
stdin/stdout outside the app package; the core imports nothing from the app package). This
banked evidence is the actual stdout of `python3 seen-red/setup-tui-purity-gate/run_fixtures.py`
against the rewritten gate, witnessed 2026-07-22:

case 1a ok: bare print() inside a non-exempt module reads red
case 1b ok: the same shape inside runner.py (the exempted choke point) reads green
case 2 ok: sys.stdin/sys.stdout attribute access in a non-exempt module reads red (2 hit(s))
case 3a ok: steps.py importing tools.configtree.app (the textual-dependent submodule) reads red
case 3b ok: steps.py importing the library's top-level types (SectionSpec/TextField) reads green -- every section module needs these to declare its data
case 3c ok: the same textual-dependent import inside tui_app.py (the one legitimate importer) reads green
setup_tui_purity_gate: clean.
case 4 ok: the rewritten gate against the REAL tools/setup_tui/ tree exits 0 (clean)
ALL CASES OK -- setup_tui_purity_gate.py's two rewritten detectors, both polarities, plus a real-tree clean confirmation

RED legs (1a, 2, 3a) prove the detectors actually catch the shape they claim to forbid, not
merely that the script runs; GREEN legs (1b, 3b, 3c, 4) prove the declared exceptions and the
real tree are not falsely caught alongside them.
