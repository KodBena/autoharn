seen-red/setup-tui-dry-run-parity -- banked RED evidence (both-polarity-adjacent proof, fixture-census)

design/FABLE-SETUP-TUI-SPEC.md's 2026-07-19 `--dry-run` amendment (commission ledger row 1719):
WDR1 and WDR2, the two witnesses this fixture proves with REAL infra (a live, reachable Postgres
host and a real boundary_service process) -- WDR3 (a hostile input refuses identically under
--dry-run) needs neither and lives in seen-red/setup-tui-scripted-smoke's case 9 instead.

This fixture is inherently REFUSAL-adjacent in the same sense fixture_census's own convention
treats a live-infra proof: a failure here (an assertion raised, the tree hash differing, the
ledger row count moving, or the two argv lists diverging) IS this gate's red case -- the dry-run
mechanism silently doing a real write, or the WOULD-DO table silently drifting from what a real
run actually executes, is exactly the defect class this fixture exists to catch.

WDR1: births a REAL scratch world (real led, real boundary_service, a real hydration pass that
writes two real led-decision rows -- ids 7 and 8 in the captured run below), then reruns
hydration a SECOND time -- the SAME catalog item, the SAME ADR, against the SAME real destination
and the SAME real, reachable led -- under `--dry-run --start-at hydration`. Checks, not assumes:
the destination's file tree (39 files) hashes identically before/after, and the world's own max
ledger-row id stays at 8 (a real write here would have produced row 9). This is a strong
proof specifically because the live counterpart of every act the dry rerun computes provably
DOES write something when actually run -- "nothing changed" is a real claim against a mechanism
proven capable of changing it, not a vacuous one against a mechanism that never could have.

WDR2: runs the SAME pinned scripted answers (preflight through boundary, a live in-process
boundary-service start) twice against the same destination path -- once live (a real scratch
birth+boundary, torn down with zero residue immediately after so the second run starts from the
identical nonexistent-destination precondition), once `--dry-run` -- and diffs the two runs'
`$ `-prefixed argv-echo lines (tools/setup_tui/runner.py's `run_command`/`start_background`,
which echo the exact argv unconditionally, live or dry) for byte equality, order included.

Requires HARNESS_PGHOST (or EPISTEMIC_PGHOST) set to a reachable cluster with a `toy` database,
and ~/w/vdc/venvs/generic/bin/python (the same venv tools/setup_tui/screens.py's own
screen_boundary hardcodes) -- absent either, the fixture prints UNEXERCISED and exits 0 rather
than failing the build on missing optional local infra; this run had both.

Real captured stdout, 2026-07-19, a clean run against this commit's tree (zero residue verified
by teardown-world.sh's own check AND by a direct pg_namespace/pg_roles/process-table sweep after
the run, exit 0):

WDR1 ok: --dry-run rehydration against a REAL destination with a REAL, connected led (/tmp/setup-tui-dry-run-parity-1dyf2zdr/dest_a) left the filesystem byte-identical (39 files checked) and the ledger row count unchanged (max id 8 before and after)
WDR2 ok: the --dry-run WOULD-DO argv list (7 commands) is byte-identical, order included, to the real scratch run's own argv list (same pinned world/dest/host/db, live run torn down with zero residue before the dry rerun)
ALL CASES OK -- setup_tui --dry-run WDR1 (byte-identical filesystem + zero new ledger rows against a real, led-connected destination) and WDR2 (argv parity vs a real scratch run), zero residue
