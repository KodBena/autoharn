seen-red/extract-context — banked RED evidence (both-polarity proof, fixture-census)

extract-context (bootstrap/extract_context.py + the repo-root `./extract-context` shim) is the
mechanized world-context extraction the world-context consult designed
(vestigial_documentation/design/FABLE-WORLD-CONTEXT-MIGRATION-CONSULT-2026-07-19.md; autoharn
ledger row 1942 step 1). This file banks two pieces of RED evidence: (A) an EXPECTED-red case
that stays part of the green suite (RED-INGEST-UNREVIEWED, a refusal the tool is SUPPOSED to
produce), and (B) a genuine DEFECT this fixture caught during this build, before it landed, with
the fix that turned it green -- both from LIVE runs against the real toy@192.168.122.1 db, using
throwaway scratch schemas (ecfxsrcfixture_scratch / ecfxtgtfixture_scratch), torn down after.

--------------------------------------------------------------------------------------------------
(A) CASE RED-INGEST-UNREVIEWED -- expected refusal, still exercised every green run

`./extract-context ingest` against a manifest with no `{"record":"review","reviewed":true,...}`
line is REFUSED WHOLESALE, exit 1, zero rows written to the target world:

  extract-context ingest: REFUSED WHOLESALE -- /tmp/ecfx/manifest.jsonl carries no reviewed
  marker ({"record":"review","reviewed":true,...}). This is the maintainer's veto surface
  (autoharn ledger row 1942) -- no item, carried or dropped, is written until a distinct
  reviewer appends that line. No write was attempted.
  EXIT=1

Verified live: target ledger row count before and after the refused attempt was identical (6),
queried directly via `SELECT count(*) FROM ecfxtgt_scratch.ledger` -- not inferred from the
tool's own exit code alone.

--------------------------------------------------------------------------------------------------
(B) A genuine defect found by this fixture during the build, fixed, banked as evidence the RED
    path is real (not a fixture that only ever passes)

First live `ingest` attempt against a reviewed manifest (before the fix below) refused EVERY
carry-verbatim/carry-reopened item whose ingestion path used the generic `led <kind>
<statement...>` form with `--refs` placed AFTER the statement -- the kernel's own CLI parser
requires standing flags like `--refs` BEFORE the kind word (`led [flags] <kind> <statement...>`),
not after the statement, and refuses loudly (never silently swallowing the flag into statement
prose) when given in the wrong position:

  {"class": "1.2_standing_decisions", "disposition": "carry-verbatim", "outcome": "DROPPED",
   "reason": "led refused: led: REFUSED -- the statement contains '--refs' as a TRAILING (or
   embedded) shell argument, which is a known led flag token. Flags are consumed by the
   top-of-file parser BEFORE the variadic <statement...> capture begins -- a flag placed after
   the statement is silently swallowed into statement prose instead of being parsed, and the
   typed column it names lands NULL with no error (ledger item
   led-refs-flag-order-parser-bug; BACKLOG autoharn1 rows 1053/1054, experience-world rows
   65/66/71/87 -- each occurrence cost a superseding rewrite).
   NOTHING was written. Flags go BEFORE the statement -- re-issue as:
     ./led [flags] decision --refs re-asserted from ecfx-src: ecfx-src:row:7 \"<statement...>\""}

  {"class": "1.5_open_questions", "disposition": "carry-reopened", "outcome": "DROPPED",
   "reason": "led refused: led: REFUSED -- the statement contains '--refs' as a TRAILING (or
   embedded) shell argument ... Flags go BEFORE the statement -- re-issue as:
     ./led [flags] question --refs re-asserted from ecfx-src: ecfx-src:row:10 \"<statement...>\""}

FIX (bootstrap/extract_context.py, `cmd_ingest`): the two call sites for classes
`1.2_standing_decisions` and `1.5_open_questions` were reordered from
`["decision", *grade_args, statement, "--refs", refs]` to
`["--refs", refs, "decision", *grade_args, statement]` (and identically for `question`) --
`--refs` now precedes the kind word, matching `led`'s own documented flag-ordering contract. Note
that `led work open`'s OWN `--refs` (a work-open-specific flag, not the generic top-of-file one)
is unaffected -- it is parsed inside `work open`'s own subcommand loop and was already correctly
positioned; only the two generic-path call sites needed the fix. Re-run after the fix: both
classes RE-ASSERTED/RE-ENACTED cleanly (see GREEN-INGEST-ATTRIBUTED in the current green suite,
seen-red/extract-context/run_fixtures.py) -- NOTHING here is a residual known-broken path; this
is banked so the fixture's own red-catching capability is provably real, not merely claimed.

--------------------------------------------------------------------------------------------------
(C) 2026-07-22 -- PRE-S36 FALLBACK, RED-then-GREEN (this repo's own autoharn1 world, ledger row
    1950's live commission)

extract-context, run live against this repo's own world (autoharn1, deployment.json at repo root),
came back for class 1.2_standing_decisions as a single class-summary: "UNAVAILABLE: this world's
kernel predates s36 (standing_decisions does not exist)" -- honest, but insufficient for a
succession whose trust condition is verbatim-with-citation carry, nothing silently dropped
(row 1942). PRE-S36-FALLBACK was added to run_fixtures.py (simulating a pre-s36 kernel by dropping
the `standing_decisions` VIEW on the scratch source world -- not birthing a whole second lineage
chain, since the production code branches solely on `_relation_exists(standing_decisions)`) and
run BEFORE `extract_standing_decisions()` gained its fallback branch:

  PRE-S36-FALLBACK: standing_decisions absent -> 0 kind=decision row(s) from ledger_current
  carried verbatim with grade=None, 1 class-summary naming the widening -- FAIL

  !! PRE-S36-FALLBACK: fallback extraction did not produce the expected carry-verbatim/no-grade
     items + class-summary (exit=0)
  decisions=[]
  summary=[{'class': '1.2_standing_decisions', 'count': 0, 'disposition': 'drop-with-reason',
  'reason': "UNAVAILABLE: this world's kernel predates s36 (standing_decisions does not exist)",
  'record': 'class-summary'}]

(the two decision rows seeded earlier in the same fixture run -- "Standing rule ALPHA" (durable)
and "ordinary undurable decision, not standing" -- were both silently absent from the manifest,
exactly the row-1942 trust-condition violation this addition exists to close.)

FIX: `extract_standing_decisions()` gained an ELSE branch -- when `standing_decisions` does not
exist but `ledger_current` does, every unsuperseded kind=decision row is read from
`ledger_current` (s31 supersedes-filtered semantics, the same source `./pickup`'s own
IN-FORCE-DECISIONS/RESOURCES/ESTIMATES sections read) and carried as a 1.2_standing_decisions
carry-verbatim item with `grade: null` (no grade concept exists pre-s36 -- never invented), plus
one class-summary line naming the fallback and its widening relative to the s36 path (it carries
ALL unsuperseded decision rows, not only the graded subset `standing_decisions` itself would
select). Re-run after the fix, same scratch world:

  PRE-S36-FALLBACK: standing_decisions absent -> 2 kind=decision row(s) from ledger_current
  carried verbatim with grade=None, 1 class-summary naming the widening -- PASS

The s36-present branch (exercised by every other case in this suite, GREEN-EXTRACT-DETERMINISTIC
through GREEN-INGEST-ATTRIBUTED) is untouched code -- same query, same item shape -- and all five
of those cases stayed green across both the RED and GREEN runs of this addition, confirming the
fix is additive-only.
