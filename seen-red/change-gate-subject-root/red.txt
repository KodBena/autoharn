seen-red/change-gate-subject-root — banked RED evidence (both-polarity proof, fixture-census)

BACKLOG "Run-2 integrity finding" (2026-07-09): run 2's `.claude/settings.json` baked an absolute
GATE_SUBJECT_ROOT/E13_SUBJECT_ROOT env var pointing at the project's PRE-MOVE path. Because
`is_governed()` matches by STRING PREFIX only (never checks the directory actually exists), a
dangling SUBJECT_ROOT made it return False for every real edit under the (different, post-move)
project path — silently governing nothing for the session's entire life.

BEFORE the fix (git show HEAD:hooks/pretooluse_change_gate.py — this fix's own edits are still an
uncommitted working-tree change, so HEAD is the byte-held pre-fix version — run standalone as a
scratch copy, real output, 2026-07-09):
  echo '{"tool_name":"Edit","tool_input":{"file_path":"/tmp/seenred-real-project-after-move/foo.py"},"cwd":"/tmp"}' \
    | GATE_SUBJECT_ROOT=/nonexistent-seenred-fixture-root python3 <pre-fix hooks/pretooluse_change_gate.py>
  exit: 0
  stdout: (nothing)
This is the exact silent-defeat shape the BACKLOG entry describes: the edited file's real path does
not start with the stale SUBJECT_ROOT string, so `is_governed()` returns False and the gate governs
nothing, with zero signal to the operator.

AFTER the fix (case a, real output, 2026-07-09):
  echo '{"tool_name":"Edit","tool_input":{"file_path":"/tmp/seenred-real-project-after-move/foo.py"},"cwd":"/tmp"}' \
    | GATE_SUBJECT_ROOT=/nonexistent-seenred-fixture-root python3 hooks/pretooluse_change_gate.py

  Ledger policy: SUBJECT_ROOT is configured as '/nonexistent-seenred-fixture-root' but that path
  does not exist on disk, so this gate cannot determine which files it governs — refusing rather
  than silently governing nothing (the run-2 integrity finding: a moved project directory left a
  stale absolute path baked into a hook command). Fix: repoint .claude/settings.json's
  GATE_SUBJECT_ROOT (or its deprecated alias E13_SUBJECT_ROOT) at the real project root, then retry.
  {"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", ...}}
  exit: 2

BACKLOG "Run-5 forensics" (2026-07-10): the ledger is a retroactive diary, not a permit log — 8m23s
of witnessed work landed as 19 ledger rows in 89.4s, after the fact, in a session the gate had
otherwise governed correctly. The fix (permit-to-work): a wired subject root whose ledger schema
carries the s22 work-item layer denies a Write/Edit unless a work item is currently OPEN and
CLAIMED, composing IN FRONT of the ticket/window logic above (both mechanisms hold at once).

AFTER the fix (case f, real output witnessed via `python3 seen-red/change-gate-subject-root/
run_fixtures.py`, 2026-07-10 — case f's own setup.sh applies high_watermark_1.sql + s20 + s21 +
s22 to a scratch schema first, so the bare command below only reproduces this once that schema
exists; the fixture harness is the reproducible entry point, not the standalone one-liner):
  echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/fixture_target_ptw_f.py"},"cwd":"/tmp"}' \
    | GATE_SUBJECT_ROOT=/tmp GATE_LEDGER=hookprobe_ptw_f.ledger LEDGER_DB=toy \
      GATE_STATE=/tmp/ptw_f_state.json GATE_JOURNAL=/tmp/ptw_f_journal.jsonl \
      python3 hooks/pretooluse_change_gate.py

  Ledger policy (s22 permit-to-work): this world's ledger carries the work-item layer and no
  work item is currently OPEN and CLAIMED — a ledger entry written after the fact is not a
  permit to work (BACKLOG 'Run-5 forensics', 2026-07-10: 8m23s of witnessed work landed as 19
  ledger rows 89.4s later — honest, but late). Open and claim a work item, THEN retry the same
  edit:
    ./led work open <slug> "<title>"
    ./led work claim <slug>
  {"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", ...}}
  exit: 2

Case g (same setup, PLUS an open+claimed work item and a matching ticket entry) proves the OPPOSITE
polarity: exit 0, no deny text at all — permit-to-work satisfied AND the pre-existing ticket
requirement satisfied. Case h proves the NAMED fail-open choice: a pre-s22 world (no
work_item_current view; the byte-held nla/public default) skips this check entirely and falls
through to the old "needs a ledger entry" deny, unchanged.

Case i (work item change-gate-foreign-defaults, 2026-07-17): pins `_under_subject_root()`'s
explicit empty-SUBJECT_ROOT guard added when the byte-held foreign-project SUBJECT_ROOT/STATE/
JOURNAL defaults were removed. An unwired invocation (no GATE_SUBJECT_ROOT/E13_SUBJECT_ROOT, no
locatable deployment.json) now resolves SUBJECT_ROOT to "" -- and the containment check
`ap.startswith(SUBJECT_ROOT + os.sep)` degenerates to `ap.startswith(os.sep)` on
SUBJECT_ROOT == "", which is TRUE for every absolute path on this platform. SUBJECT_ROOT="" must
govern NOTHING; without the guard, an unwired session would treat EVERY file on disk as "under"
the (unset) subject root -- the opposite of the module docstring's unwired posture and a straight
regression from the foreign default's own accidental safety (a real, but irrelevant-elsewhere,
directory that almost nothing lived under). Case i exercises exactly that shape: unwired env, a
*.py Edit target, exit 0 and no deny text.

Full nine-case run (red + healthy-subject-root + unwired-unset + deprecated-alias + Bash-path-
scope + permit-to-work no-open-claimed/open-claimed-allowed/pre-s22 + unwired-py-allowed, all
asserted programmatically; f/g apply-and-drop a scratch s22 schema in the toy db per run, zero DB
residue):
  python3 seen-red/change-gate-subject-root/run_fixtures.py
