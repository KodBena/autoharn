# Sources — LOCAL ONLY (copyright)

The source PDFs backing this brief live locally at
`~/w/vdc/1/local-sources/incomplete-evidence-sources/` (ISA 230/500, IESBA Code,
ISO 15489-1, MHRA GxP). Never committed; `.gitignore` forecloses the class.
Same-hour expunge history as the sibling brief — see BACKLOG.md.
